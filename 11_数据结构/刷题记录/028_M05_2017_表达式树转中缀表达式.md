---
tags:
  - "408"
  - 数据结构
  - 刷题
题号: 28
母题: "M05"
母题模块: "二叉树遍历、递归与构造"
章节: "树·算法设计"
来源: "408真题"
引用: "2017"
标题: "表达式树转中缀表达式"
优先级: "S+"
限时: 20
手写: "必须"
训练目标: "使用中序递归输出表达式，并严格控制必要括号。"
状态: "未开始"
首刷日期: ""
首刷耗时: ""
首刷结果: "未完成"
二刷日期: ""
二刷结果: "未完成"
纸笔日期: ""
纸笔得分: ""
错误类型: "无"
下次复习: ""
复习次数: 0
掌握评分: ""
薄弱点: ""
备注: ""
---

# 表达式树转中缀表达式

> [!info] 题目卡片
>
> | 字段 | 内容 |
> |---|---|
> | 来源 | 408真题 `2017` |
> | 母题 | [[M05_二叉树遍历、递归与构造]] |
> | 章节 | 树·算法设计 |
> | 优先级 | **S+** |
> | 建议限时 | 20 分钟 |
> | 手写要求 | 必须 |



## 📋 真题题面

> [!info] 2017 年第 41 题 · 表达式树转中缀表达式
> 综合题 · 15 分 · 考点：树与森林、中序遍历

请设计一个算法，将给定的表达式树（二叉树）转换为等价的中缀表达式（通过括号反映操作符的计算次序）并输出。

**示例**：下列两棵表达式树作为算法输入时，输出的中缀表达式分别为 `(a+b)*(c*(-d))` 和 `(a*b)+(-(c-d))`。
- 表达式树 1：根为 `*`，左子树根 `+`（左叶 `a`、右叶 `b`），右子树根 `*`（左叶 `c`、右子树根 `-`，其右叶 `d`）
- 表达式树 2：根为 `+`，左子树根 `*`（左叶 `a`、右叶 `b`），右子树根 `-`（右子树根 `-`，其左叶 `c`、右叶 `d`）

> 注：单目运算符 -（取负）在树里以左子树为空、右子树为操作数表示。

二叉树结点的定义如下：
```c
typedef struct node {
    char data[10];   // 存储操作数或操作符
    struct node *left, *right;
} BTree;
```

**要求**：
(1) 给出算法的基本设计思想。
(2) 根据设计思想，采用 C 或 C++ 语言描述算法，关键之处给出注释。

> [!tip]- 💡 思路提示（先自己思考再展开）
> 中序遍历输出，但需根据结点类型与操作符优先级决定是否加括号：叶子结点直接输出；非叶子结点左右子树需加括号，单目负号单独处理（左子树为空）。

---

## ⚡ 状态快速切换

```dataviewjs
// 点击按钮一键更新状态（写入 frontmatter，保存后总览自动刷新）
const statuses = ['未开始', '学习中', '已AC', '已二刷', '可纸笔'];
const colors = {'未开始':'#9ca3af','学习中':'#3b82f6','已AC':'#22c55e','已二刷':'#14b8a6','可纸笔':'#f59e0b'};
const cur = dv.current();
const root = dv.el('div', '');
root.createEl('style', {text: `
  .dsq{display:flex;gap:6px;flex-wrap:wrap;align-items:center;}
  .dsq-btn{padding:4px 14px;font-size:13px;border-radius:14px;border:1px solid var(--background-modifier-border);cursor:pointer;background:var(--background-primary);color:var(--text-normal);user-select:none;transition:all .15s;}
  .dsq-btn:hover{transform:scale(1.05);border-color:var(--interactive-accent);}
  .dsq-cur{font-weight:700;color:#fff !important;}
  .dsq-tip{font-size:11px;color:var(--text-muted);margin-top:6px;}
`});
root.innerHTML = `<div class="dsq">
  ${statuses.map(s => `<button class="dsq-btn${cur.状态 === s ? ' dsq-cur' : ''}" style="${cur.状态 === s ? 'background:' + colors[s] + ';border-color:' + colors[s] + ';' : ''}" data-s="${s}">${s}</button>`).join('')}
</div><div class="dsq-tip">点击上方按钮修改本笔记状态，保存后总览页与母题页自动更新。</div>`;
root.addEventListener('click', async e => {
  const btn = e.target.closest('button[data-s]');
  if (!btn) return;
  const s = btn.dataset.s;
  const tf = dv.app.vault.getAbstractFileByPath(cur.file.path);
  await dv.app.fileManager.processFrontMatter(tf, fm => { fm['状态'] = s; });
  // 立即更新当前面板高亮，避免等待索引刷新导致"点两次才生效"
  btn.classList.add('dsq-cur');
  root.querySelectorAll('.dsq-btn').forEach(b => { b.classList.remove('dsq-cur'); b.style.background=''; b.style.borderColor=''; });
  btn.style.background = colors[s]; btn.style.borderColor = colors[s];
});
```


## 🎯 训练目标

使用中序递归输出表达式，并严格控制必要括号。

## 📊 状态追踪

> 当前状态：**未开始**
> 修改顶部属性（状态/首刷结果/纸笔得分等）后保存，总览页会自动重新统计。

- [ ] **首刷** — 日期：—，耗时：— 分钟，结果：未完成
- [ ] **二刷** — 日期：—，结果：未完成
- [ ] **纸笔** — 日期：—，得分：—/10

## 📝 解题笔记

（记录算法思想、关键代码、复杂度分析、易错点）

## 📚 错题复盘

- **错误类型**：无
- **薄弱点**：—
- **复习次数**：0，**下次复习**：—
- **备注**：—
