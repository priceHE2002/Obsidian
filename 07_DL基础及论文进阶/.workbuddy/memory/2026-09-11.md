# 2026-09-11

## 翻译《Algorithms for Modern Hardware》全书入 Vault

- 源：https://en.algorithmica.org/hpc/，作者 Sergey Slotin（开源，github.com/algorithmica-org/algorithmica）。
- 决定包含 82 篇已发布章节 + 45 篇仓库内未上线草稿章节（parallel / distributed 两章）。
- 落位：Vault 顶层新建 `05_现代硬件算法/`，下设 14 个编号章目录 + `99_附录/`。
- 流程：
  1. 用 `build_stage.py` 从仓库 `content/english/hpc/` 抽取 127 个 md、按章归位、改写内部链接为 Obsidian wikilink（带 `__An__` 占位锚点）、按章复制 137 张图片到对应 `img/`。原著 17 处失效/笔误链接全部显式修复（含旧页面名、错路径、HTML 注释屏蔽）。
  2. 翻译约定 `CONVENTIONS.md` 定义 frontmatter、保留项、术语对照表。
  3. 16 批并行 general-purpose 子代理按章翻译输出到 Vault。
  4. 跨页锚点 `__An__` 后处理（仿 GitHub slug + 去重），全部 0 残留。
  5. MOC 索引 `00_导读/现代硬件算法·总览.md` 与 `README.md` 生成。

- 结果：`05_现代硬件算法/` 下 127 个 md（含 1 个 README）+ 135 张图片，跨章 wikilink 全部命中、跨页锚点全部解析、原著占位/空页（如 distributed/*、management.md 等）按 CONVENTIONS.md §8 输出占位说明。

## 深度 review（第二轮）

- 结构比对：标题层级（含"新增 h1"与"整树提升一级"两种合法模式）、代码块配对、`$$`/`$` 公式逐条 multiset 对比、图片与 wikilink 计数——全部通过。
- 修复的问题：
  1. `哈希表.md` 标题层级不一致（源 `##`+`###` 被提升为 `#` 后子级未跟着提升）→ 已修；
  2. `概率论速览.md` 生日悖论公式丢了 `\ldots` → 已补回；
  3. `IEEE 754 浮点数.md` / `整数.md` 两处英文表头 → 已汉化；
  4. 7 处英文图片 alt（二分查找/静态 B 树/线段树）→ 已汉化；
  5. **最重要**：第一版锚点脚本 slug 匹配有 bug，走了"全局兜底"分支，24 页的跨页锚点解析到了错误标题 → 用"文档顺序 + manifest 元数据"位置配对的方式整体重解，62 条带锚 wikilink 全部命中目标页真实中文标题。
- 保留不动的（核查后确认合法）：HTML 注释里的英文草稿（吞吐量计算/搜索树/数据搬移/静态 B 树）、书名与标准名英文、`[[likely]]`/`[[unlikely]]`（C++ 属性，行内代码内）、`O(\log n` → `O(\log n)`（修正源文未闭合）。
- 工程教训：
  - `image copy` 块漏写 `OUT` 前缀，导致图片复制到了 `/tmp/hpc_build/` 而非 Vault；补一次就修正。
  - 跨页锚点 slug 需用 GitHub 风格（小写、保留 `_`、空白→-、重名加 `-1`）；**兜底分支会静默解析到错误页面**，宁可失败也不要兜底，或最后用位置配对整体重解一遍。
  - C++23 `[[unlikely]]` attribute、Markdown 表格列名等是英文是允许的；不在 `english_residue` 误报范围内。
  - 校验"整段漏译"时，`<!-- -->` 剥离要用状态机（`find`）而非非贪婪正则；`$$` 块要先抽再抽行内 `$`，否则互相干扰计数。
## 新技能：math-derivation-style

- 用户指定以书中浮点乘法推导（`浮点数.md` 的 aligned 公式）为唯一基准，要求今后所有数学推导按此风格书写。
- 已创建用户级技能 `~/.workbuddy/skills/math-derivation-style/SKILL.md`（含基准示例、反例、自检清单），
  并在 `~/.workbuddy/MEMORY.md` 顶部记为硬性偏好：≥2 个等号必须 `$$+aligned` 等号对齐逐行推导、`\cdot` 乘号、
  中间量用 `\underbrace/\overbrace` 就地命名；单等式不套用。
- 按"以后"要求，未对已翻译的 127 页做回溯改写。
