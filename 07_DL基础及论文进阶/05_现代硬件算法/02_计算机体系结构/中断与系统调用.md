---
tags:
  - 现代硬件算法
  - 计算机体系结构
created: 2026-09-11
source: https://en.algorithmica.org/hpc/architecture/interaction/
original_title: Interrupts and System Calls
---

# 中断与系统调用

```asm
global _start

section .text

_start:
  mov rax, 1        ; write(
  mov rdi, 1        ;   STDOUT_FILENO,
  mov rsi, msg      ;   "Hello, world!\n",
  mov rdx, msglen   ;   sizeof("Hello, world!\n")
  syscall           ; );

  mov rax, 60       ; exit(
  mov rdi, 0        ;   EXIT_SUCCESS
  syscall           ; );

section .rodata
  msg: db "Hello, world!", 10
  msglen: equ $ - msg
```

中断的代价很高。它们本不应出现在正常的执行路径上。例外。

进行系统调用会带来一定的开销，因此通常应尽量避免。例如，所有 I/O 通常都会被缓冲，这样你只需向操作系统发送一整块（比如 4KB）数据。
