# 2026-09-12

## 生词默写本 v3：去掉默写区 + 重排（用户要求"去除默写区还有整理排版"）

改写 `05_英语/英语生词默写本_Obsidian_411词_v2.md`（2569 行 → 1908 行），同一路径覆盖。
`05_英语/英语生词默写本_排版模板.md` 同步升级到 v3 规范。

### 用户偏好（重要，以后照做）
- **不要在 Obsidian 生词本里放默写区**。空列 / 打分行 / "我的默写"表格一律不要 ——
  用户在 Obsidian 里不会往表格填字，**卡片收起时只剩英文标题，本身就是题面**。
- 加词只需追加一张卡片，不要设计成"要同步维护两张表"。

### v3 结构
- 标题层级：文档标题 `#`（全书唯一）→ 单元 `##` → 小节 `###`（词条卡 / 错词）。
  这样大纲面板整齐、`[[#Unit 01 · 主题]]` 跳转有效。v2 把 11 个单元都写成 H1 是错的。
- 卷首：使用说明 + 待核对词 + 总进度表（`Unit | 词号 | 主题 | 默写 | 订正 | 复习`，只剩 3 个 checkbox）。
- 每单元：Unit Card（范围/收录/进度）→ `### 词条卡`（40 张 `> [!vocab]-`）→ `### 错词`
  （`> [!failure]-` 里放一行 `- **word** — 错记成 … → 正确义：…` 骨架，不用表格）。
- 已删除：题干区表、"我的默写/订"列、复习记录打分表、Unit 11 的 20 个自补位空行。

### 校验与坑
- 回读校验：411 → 411，missing/extra 0，mismatches 0。
- **标题里的星号必须转义**：`> [!vocab]- 328 · satire\*`，否则 `*` 会被 Markdown 当强调符。
  校验脚本要先把 `\*` 还原再比对，否则会误报 1 条。
- 单元之间的 `---` 前后必须留空行，紧跟文本的 `---` 会被解析成 setext 二级标题。

### 技能同步
`~/.workbuddy/skills/obsidian-vocab-card-book/` 已整体升级到 v3：SKILL.md 规范段、
`scripts/convert_to_card_layout.py` 的 build()（现在输出 H1+H2 结构、无默写区）、
`scripts/verify_conversion.py`（还原 `\*`、去掉题干区行数断言、加 H1/H2 结构体检）。
两个脚本重跑通过。

## 三合一 + 修截图里的两个渲染 bug（同日上午）

用户发了张 Obsidian 渲染截图：总进度表塌了。诊断出两个真实 bug：

1. **表格里的 wikilink 竖线没转义**：`[[#Unit 01 · 主题|Unit 01]]` 在表格里会被先按 `|`
   切单元格 → 链接散架、列宽炸掉。**正确写法 `[[#Unit 01 · 主题\|Unit 01]]`**。
   最终方案是**干脆不用表格**装进度：改成任务列表 `- [ ] [[#Unit 01 · 主题]]　001–040`
   （无别名时不需要竖线，链接直接显示标题文字）。
2. **`- [ ]` 被反引号包住 + 不在行首** → 只渲染成纯文本，点不动。
   必须写成真正的任务项：`> - [ ] 默写`（三细项并列就写三行，一行里塞多个勾选框只认第一个）。
   两种写法都清干净了，成品里 `` `- [ ]` `` 出现 0 次。

### 合并结果
三个文件并成 **一个** `05_英语/英语生词默写本_411词.md`（2004 行 / 43KB）：
正文（11 Unit 折叠卡）+ 文末 `## 附录 · 排版规范与录入`（字段规范 / 单元结构 /
录词骨架 / 折叠语法速查 / 可选 CSS，原模板内容全部并入）。
原三个文件移到 `05_英语/.archive/2026-09-12/`（**未删除**，随时可取回）：
旧版表格本、v2 折叠本、排版模板。

校验：411 → 411，missing/extra 0，mismatches 0，H1=1，H2=13。

### 技能再同步（v4）
- `convert_to_card_layout.py`：build() 改成任务列表进度 + 真任务项 Unit Card；
  新增 `--appendix` 开关，产出单文件成品（不再需要独立模板文件）。
  两种模式各跑一遍：H2=13、mismatches 0。
- `verify_conversion.py`：新增 `strip_fences()`，跳过 ``` 与 `> ``` ` 围起来的内容，
  否则附录里的示例卡片会被误判成第 412 个词。
- SKILL.md 加了「三个坑」小节，把上面两个渲染 bug 与星号转义并列写死。

## 删「源」+ 补例句（同日上午，用户连续两条要求）

### 1) 删除 `**源**` 字段
`原/手/补` 的释义来源标记全部删掉（411 行 + 附录字段表那一行 + 两处示例骨架），
卷首图例同步改写。用脚本按 `^(> )+**源**` 匹配删除，删后无连续空行残留。

### 2) 新增 `**例**`：真题优先
**从真题 PDF 挖句子**（关键突破：`考研英语一阅读真题_A4装订版.pdf` 有文字层）：
- 用 pypdf 提取 → 靠页头 `20XX 年…Text N (PASSAGE|QUESTIONS)` 切正文页
  → 去页眉页脚 → 跨行连字符合并 → 句点+大写切句，得到 **64 篇 / 1189 句**。
- 装 pypdf 到 `/Users/heyuhang/.workbuddy/binaries/python/envs/default`。
- 词形变体匹配（s/es/ed/ing/双写/y→ies）：**185/411 命中真题原句**，句尾标 `（2016 · Text 1）`。
- 剩余 **226 条手写例句**（按各自义项写，命中词加粗）。
- **人工复核发现三类坑**：①撞专名（`rage`→"Rage Against the Haze"、`tan`→人名 Tan Kiat How）；
  ②义项不合（`equity` 释义是「公平」却匹配到 equities「股票」→ 换用 "promote gender equity"；
  `account` 避开 accounting）；③只出现在复合词里（`waist`→wasp-waist、`greet`→greeting-card、
  `sober`→sober-sided）。这三类都改自撰句。
- **换句必须同步改出处**：`equity` 换了句子却沿用了旧匹配的「2019 · Text 1」，
  实际那句出自 2020 Text 3，已修正 —— 这类错最隐蔽，一定要单独核。
- 自撰句里 `184 tan` 释义是 `vi. 晒黑` 却写成了名词 a deep tan，也已改成动词。

### 3) 技能升级到 v5
- 新增 `scripts/mine_exam_sentences.py`（PDF→正文页→句子→词形匹配→examples.json，`--report` 打未命中清单）。
- `convert_to_card_layout.py` 新增 `--no-source`、`--examples examples.json`（带 cite 自动标出处）。
- `verify_conversion.py`：新文件里没有 `源` 行时自动跳过来源比对；顺带统计例句行与真题出处条数。
- SKILL.md 增「配真题例句」一节，写死上面三类坑与「换句要改出处」。
- 端到端重跑：导出例句 → 重新生成 → 校验，411/411、mismatches 0、真题 185 条，与手工成品一致。

成品最终：`英语生词默写本_411词.md` 2004 行，卡片 411 张（每张都有 义 + 例），
源残留 0，真题出处覆盖 2010–2025、最密集是 2016–2019。

## 释义按考研取向逐条重写（同日）

用户要求「再检查优化一下每个单词的释义，使其更贴合考研要求」。411 条释义全部重写。

诊断：Unit 01–05（001–200）是词典义项照搬，问题最多；Unit 06–11 质量尚可，只做统一化。
典型毛病与处理：
- `round` 原列 10 个义（含「轮唱」「一局」）→ 收敛为 `n. 一轮；一回合；圆形物　·　adj. 圆的；整数的`；
- `worship` 原写「崇拜者, 敬神者」——那是 worshipper → 改为 `n. 崇拜；敬仰　·　v. 崇拜；敬奉`；
- `overrule` 原写「统治,支配」→ `vt. 否决；推翻（裁决）`；
- `hinder` 原标 `adj. 后面的` → `vt. 阻碍；妨碍`；
- `concentrate` 原只留 `n. 浓缩, 精选` → `v. 集中；专注；浓缩　·　n. 浓缩物`；
- `reproduce` 原只写「生殖」→ `v. 复制；再现；繁殖`；
- `quo` 原写「[古]说」→ `n. 现状（多用于 status quo）`；
- 补熟词僻义：`address` +vt. 处理、`sole` +adj. 唯一的、`faint` +adj. 微弱的、`account` +vi. account for；
- 补缺：`079 contemptuous`（原资料空缺）补「轻蔑的；藐视的」，卡内 `**疑**` 与卷首标注**此义为补录**。

格式规范（已写进文件附录 + 技能）：常考义优先；每个词性 ≤3 义；义项间 `；`；
多词性用 `　·　`；单一词性把词性提到卡片标题（脚本 `split_pos()` 自动做）。
结果：411 条全部有义、无空值、无标题/正文词性重复，最长义 38 字。

### 可复现性
导出到 `.archive/2026-09-12/`：`defs.json`（411 条，**必须带词性前缀**）、`examples.json`。
技能 `convert_to_card_layout.py` 新增 `--defs`。**踩坑**：第一次导出 defs 时只存了正文释义、
漏了标题里被提上去的词性 → 重生成 298 条词性丢失；把词性并回字符串后重测，
411/411 逐条零差异。以后从成品反推输入数据，记得把「提上去的信息」并回去。

## 统一卡片标题 + 词性进正文（同日，用户截图指出）

截图里卡片标题形态不齐：一部分是 `001 · notion · *n.*`，一部分只有 `001 · misuse`
（多词性那批，词性留在义里）。用户要求统一，并把词性放进隐藏区。

- 处理：**所有标题改为「编号 · 单词」**，词性一律写进 `义` 开头（`n. 概念；观念；看法`）。
  298 张有词性的卡片把词性从标题搬回正文，113 张多词性卡片本来就是这个形态。
- 现在 411/411 标题都匹配 `^> \[!vocab\]- NNN · 单词$`，无例外。
- 卷首、附录字段规范、附录示例骨架、释义规范全都同步改写（不再提「提到标题」）。

### 词性正确性复核（411 条全过）
- 分布：`n.` 262 · `adj.` 101 · `vt.` 89 · `v.` 37 · `vi.` 28 · `adv.` 4 · `prep.` 3 · `pl.` 1。
- 校正 2 处：`203 concede` 原标 `vt.` → 改 `v.`（及物/不及物都常用）；
  `188 saw` 补注「（又是 see 的过去式）」——熟词僻义。
- 判据：及物不及物都常用的写 `v.`；只有及物的才写 `vt.`；实词常见第二词性要补；
  复数词条（astronomers/luddites/optimists）标 `n.` 并在释义里注明复数。

### 技能 v6
`convert_to_card_layout.py`：`build()` 加 `pos_in_title` 参数（**默认 False**，词性留正文），
CLI 加 `--pos-in-title` 才恢复旧行为；附录规范行、示例骨架同步更新。
`defs.json` 重新导出（含 203/188 的修正）后复测：411/411，逐条差异 0。

## 新增「固定搭配」模块 + 卷首确认不要（同日）

### 1) 卷首说明**已确认删除，不要再加**
发现 `> [!info] 怎么用` 与 `> [!warning] 待核对词` 两块（共 16 行）从文件里消失，
一度以为是脚本 bug，排查后用户说明：**是他自己删的，卷首不要了**。
- 处理：把两处悬空引用改掉 —— `328 satire` 卡内 `**疑**` 改为自带说明
  （「原图疑为 sative，旁注『v. 讽刺』」），附录 `[!danger]` 里「在卷首登记」改为
  「卡内用 `**疑**` 写清楚，**不设卷首清单**」。
- `convert_to_card_layout.py` 的 `build()` **不再输出卷首 callout**。
- 教训：文件被外部改动后不要急着"修复"，先问清楚。

### 2) 固定搭配模块（`**搭**`，194 条）
- 语料挖不动：1189 句太少，`V+词+介词` 统计阈值降到 ≥2 只剩 12 个词有结果
  —— **改为按考研常考清单写，用语料回查印证**。
- 写法：「短语（简短释义）」，`　·　` 分隔、≤3 条；**没有搭配的词不加这一行**。
  例：`account for（解释；占）　·　take account of（考虑到）　·　on account of（由于）`
- 排序优先级：义项独立的搭配（`ward off`／`tide over`／`nip sth in the bud`）→
  介词搭配（`adhere to`／`insist on`／`be susceptible to`）→ 固定说法（`on the brink of`／`in the flesh`）。
- 只在这种搭配里有意义的词必收：`quo` → `status quo`；`bail` → `release sb on bail`、`bail out`。
- 自动校验：194 条编号都存在、每条都含目标词。

### 3) 技能 v7 + 数据同步
- `convert_to_card_layout.py` 新增 `--collocations`；附录加「固定搭配规范」note；
  字段规范 `搭` 行改写；`疑` 文案去掉卷首指向。
- 导出 `.archive/2026-09-12/collocations.json`（194 条）。
- 复测：`--defs + --examples + --collocations --no-source --appendix` 重建，
  **卡片逐条差异 0**（411/411）。
- 最终：`英语生词默写本_411词.md` 2190 行，卡片 411 张（义 411 / 搭 194 / 例 411）。

## 把技能做成成品（同日晚些，用户："请你制作 obsidian 单词本 skill"）

技能本会话里已经边做边建了，这次是**从"长出来的草稿"重构成正式技能**。

### 重构原因：旧 SKILL.md 自相矛盾
一路追加版本段落（v3/v4/v5/v6/v7），前面几段还写着已经废弃的规则：
「标题只放编号·单词·词性」（v6 已改）、「词性提取到标题」（已改）、
「来源标记原样搬到 `源`」（源已删）、「在卷首 `[!warning]` 登记」（卷首已删）。
结构也乱：字段说明散在两处、硬约定夹在中间。

### 新结构（按 skill-creator 规范，progressive disclosure）
```
SKILL.md                      ← 用途边界 / 目标版式 / 四步工作流 / 脚本表 / 参考指针
references/layout-spec.md     ← 版式细节 + 7 条 Obsidian 渲染坑 + 遮挡技巧
references/content-rules.md   ← 释义 / 搭配 / 例句 的写作规范与反例
references/workflow.md        ← 端到端流程 + 三份 JSON 契约 + 故障排查表
references/css-snippet.md     ← 可选 CSS（保留）
scripts/convert_to_card_layout.py   转换 / 新建 / 重建
scripts/verify_conversion.py        与旧本逐条比对
scripts/mine_exam_sentences.py      真题 PDF 挖句
scripts/check_book.py               ★新增：静态体检
```
description 改成第三人称 + 明确触发词（生词本/单词本/默写本/词条卡/配例句/检查排版）。

### 新增 check_book.py（不依赖旧文件的自检）
11 项：H1 唯一、H1 后不接 callout（卷首已废弃）、标题统一、`义` 以词性开头、
无 `源` 字段、`搭` ≤3 条且含目标词、例句出处格式、反引号复选框、表格未转义竖线、
setext 风险、连续空行、编号重复/缺口。`--wip` 让"缺例句"降级为提示。

**写这个脚本时自己踩的坑**（已修）：① 附录代码块里的示例卡被当成真实词条 →
需先挖空围栏（`fence_mask`）；② Unit Card 里的 `**范围**` 被算成卡片的未知字段 →
遇到 `> [!` 就退出卡片上下文；③ YAML frontmatter 的收尾 `---` 被误判成 setext 风险 →
跳过 frontmatter；④ 连续空行检查跑在"挖空后"的行上 → 改用原始行。

### convert 新增 `--from-list`（从零建本）
支持 TSV/CSV/txt：`编号<tab>单词<tab>释义`，`# 主题名` 可切单元，编号缺省自动补。
另修：单元只有一个词时范围显示 `005` 而不是 `005–005`。

### 验收
四个脚本端到端跑通：体检成品（2198 行 / 411 卡 / 194 搭 / 185 真题句，**错误 0**）、
解析旧本、三 JSON 重建、回读校验 `mismatches 0`；
**重建件与现成品卡片级差异 0**。

### 安全自查（新技能落盘例行）
无 subprocess / os.system / eval / 网络请求 / 删除类调用；全库只有两处写文件，
都写向命令行指定的 `--dst` / `--out`；不含任何凭据、不触碰 `~/.workbuddy/MEMORY.md`。

### 其他
用户在 10:26 自己整理了目录：成品移到 `05_英语/单词/` 下，`.archive` 留在上级。

