## 〇、多学会画一些二元函数
![[空间图形（一定要会画）.pdf]]

## 一、基本概念

1. **邻域 (Neighborhood)**
    - $\delta$ **邻域** $U(P_0, \delta)$：以 $P_0(x_0, y_0)$ 为中心，$\delta$ 为半径的开圆盘（不含边界）。
    - **去心** $\delta$ **邻域** $\mathring{U}(P_0, \delta)$：$\delta$ 邻域去掉中心点 $P_0$。
    - 一元函数 $\delta$ 邻域是直线段上的一个开区间，二元函数 $\delta$ 邻域是一个开区域圆，不算边界上的点
2. **二重极限 (Double Limit)**
    - **定义**：$\lim_{(x,y) \to (x_0, y_0)} f(x,y) = A$；$\lim_{\substack{x \to x_0 \\ y \to y_0}} f(x,y)$ ==>表示 $(x,y)$ 这个点可以从**任意路径**（不只是沿着坐标轴）趋向于 $(x_0, y_0)$。
    - **【考点】极限的唯一性与路径无关性**：
        - 二重极限 $P(x,y) \to P_0(x_0, y_0)$ 的趋近方式有无穷多种。
        - 如果极限存在，则沿 _任意_ 路径趋近，极限值都必须相同且存在。
        - **判断极限不存在的方法**：找两条不同的路径（例如 $y=kx$, $y=x^2$），如果算出的极限值不相等，或者某条路径的极限不存在，则原**二重极限不存在**。（例13.1）
    - PS：$(x,y) \to (0, 0)$ 可以 $x=0 ，y \to 0$ 相当于沿着 $x$ 轴靠近 (0, 0)
    - **性质**：一元函数极限的唯一性、局部有界性、保号性、运算法则、夹逼准则、等价无穷小替换等~={red}**⚠️洛必达法则除外**=~都适用于二重极限。
    - **做题总结**：计算二重极限一般会用到夹逼准则, 夹逼准则很多是趋于 0 的，很多会加上绝对值
    - 注意二重极限和累次极限Iterated Limit的区别
	    - 对于 $\lim_{x \to x_0} \lim_{y \to y_0} f(x,y)$，先固定  (视为常数)，计算 $\lim_{y \to y_0} f(x,y)$，若不存在，则累次极限不存在；若存在再计算 $\lim_{x \to x_0} \left[ \lim_{y \to y_0} f(x,y) \right]$，若不存在，则累次极限不存在，若存在，则累次极限存在，可以用一元函数极限（一般提到极限就是默认一元函数极限的）的方法计算
	    - 二重极限的存在性比累次极限的条件更强。
		    - 二重极限存在==>两个累次极限一定存在且三者相等。
		    - 累次极限存在==>二重极限不一定存在。
3. 二元函数**连续 (Continuity)**
    - **【考点】定义**：若 $\lim_{(x,y) \to (x_0, y_0)} f(x,y) = f(x_0, y_0)$，则称 $f(x,y)$ 在点 $(x_0, y_0)$ 连续。
    - **性质**：多元初等函数在其定义区域（定义域内部）上是连续的。
    - 单变量连续：函数 $f(x,y)$ 在一点处关于变量 $x$ 或关于变量 $y$ 的连续性：
	    - 若 $\lim_{x \to x_0} f(x, y_0) = f(x_0, y_0)$，则称函数 $f(x,y)$ 在点 $P_0(x_0, y_0)$ 处关于变量 $x$ 连续；
	    - 若 $\lim_{y \to y_0} f(x_0, y) = f(x_0, y_0)$，则称函数 $f(x,y)$ 在点 $P_0(x_0, y_0)$ 处关于变量 $y$ 连续.
4. **偏导数 (Partial Derivatives)**
		PS：导数本质是研究变化率，一元函数导数只能沿着一个确定的方向（即 $x$ 轴的方向），二元函数可以沿平面内任意一个方向，三元函数可以沿空间内任意一个方向。而偏导数是方向导数的一个特例，沿着特定的方向。
    - **【考点】定义（在点** $(x_0, y_0)$ **处）**：
        - 对 $x$ 的偏导：$f_x'(x_0, y_0) = \lim_{\Delta x \to 0} \frac{f(x_0 + \Delta x, y_0) - f(x_0, y_0)}{\Delta x}$。含义：对 $x$ 的偏导即 $f(x, y)$ 沿着轴方向随着自变量增量，函数值增量的变化率。**偏导数存在**即变化率存在，即比值的极限要存在。偏导数连续即 $\lim_{(x,y) \to (x_0, y_0)} f_x'(x,y) = f_x'(x_0, y_0)$，要研究 $f_x'(x,y)$ 在 $(x_0, y_0)$ 的领域里面的
        - 对 $y$ 的偏导：$f_y'(x_0, y_0) = \lim_{\Delta y \to 0} \frac{f(x_0, y_0 + \Delta y) - f(x_0, y_0)}{\Delta y}$。
        - **计算方法**：求 $f_x'$ 时，将 $y$ 视为常数，对 $x$ 求导；求 $f_y'$ 时同理。
    - **几何意义**：$f_x'(x_0, y_0)$ 是曲面 $z=f(x,y)$ 被平面 $y=y_0$ 所截曲线在点 $P_0$ 处切线（对 $x$ 轴的斜率）; 沿着 $x$ 轴方向/平形 $XoZ$ 面切一个面
    - **高阶偏导数**：（round $d$ --> $\partial$）
        - $\frac{\partial^2 z}{\partial x^2} = f_{xx}''$
        - $\frac{\partial^2 z}{\partial y^2} = f_{yy}''$
        - $\frac{\partial^2 z}{\partial x \partial y} = f_{xy}''$ （先对 $x$ 求偏导，再对 $y$ 求偏导）$f$ 下角标第二个是第二次对什么求导
        - $\frac{\partial^2 z}{\partial y \partial x} = f_{yx}''$ （先对 $y$ 求偏导，再对 $x$ 求偏导）
    - **【考点】混合偏导数相等定理**：如果两个二阶混合偏导数 $f_{xy}''$ 和 $f_{yx}''$ 在某区域 $D$ 内都**连续**，则在 $D$ 内 $f_{xy}'' = f_{yx}''$。⚠️连续才相等，初等函数在其定义区域上一定连续
5. 多元函数极值：
6. **可微 (Differentiability)**
    - **【考点】定义**：函数 $z=f(x,y)$ 在 $(x_0, y_0)$ 的全增量 $\Delta z = f(x_0+\Delta x, y_0+\Delta y) - f(x_0, y_0)$ 可表示为： $\Delta z = A\Delta x + B\Delta y + o(\rho)$，其中 $\rho = \sqrt{(\Delta x)^2 + (\Delta y)^2}$。则称 $f$ 在 $(x_0, y_0)$ 处可微，称~={red} **$dz = A\Delta x + B\Delta y$ 为全微分。=~**
	    - 全增量 - 全微分 = $o(\sqrt{(\Delta x)^2 + (\Delta y)^2})$
    - **【考点】必要条件**：若 $f$ 在 $(x_0, y_0)$ 处可微，则 $f$ 在该点的偏导数 $f_x'(x_0, y_0)$ 和 $f_y'(x_0, y_0)$ _必定存在_，且 $A = f_x'(x_0, y_0)$，$B = f_y'(x_0, y_0)$。
        - ~={red}全微分写为 $dz = \frac{\partial z}{\partial x} dx + \frac{\partial z}{\partial y} dy$。=~
    - **【考点】充分条件**：若 $f$ 的偏导数 $f_x'(x,y)$ 和 $f_y'(x,y)$ 在 $(x_0, y_0)$ 处**连续**，则 $f$ 在 $(x_0, y_0)$ 处**可微**。
    - **【考点】偏导、可微、连续的关系~={red}（重点！）**=~
        - (1) $f_x', f_y'$ 连续 $\implies$ $f$ 可微。
        - (2) ~={red} $f$ 可微 $\implies$ $f$ 连续且 $f_x', f_y'$ 存在。=~
	        - 可微 $dz$ 全微分的 $dz = \frac{\partial z}{\partial x} dx + \frac{\partial z}{\partial y} dy$ 不一定存在，但是如果给一个全微分的式子，且偏导数 $\frac{\partial z}{\partial x}$ 和 $\frac{\partial z}{\partial y}$ 是多元初等函数，则偏导数一定连续。若偏导含有参数可以用混合偏导数在偏导数连续是相等来**解**。
        - (3) $f$ 连续 $\not\implies$ $f$ 可微（反例：$z=\sqrt{x^2+y^2}$ 在(0,0)）。
        - (4) $f_x', f_y'$ 存在 $\not\implies$ $f$ 可微（反例：例13.8中的 $z_2$）。
        - (5) $f_x', f_y'$ 存在 $\not\implies$ $f$ 连续。
		🤨自我理解：二元函数可导的定义只是要求“偏导数存在”【只保证了和 $y$ 两个方向是“光滑”的，但其他方向（比如 $y=x$ 方向）完全可能存在“尖刺”或“断崖”】而可微则要求增量写成 $x$ 方向增量和 $y$ 方向增量以及 $x、y$ 增量的高阶无穷小，所以仅仅偏导数存在不可以。
		二元函数偏导连续是最强的，可微其次，可导最弱
		🐱二元函数可微意味着这个二元函数 $z=f(x,y)$ 在 $(x_0, y_0)$ 这一点是**局部光滑**的，光滑到可以用一个平面（切平面）去近似它
		记 $f(x,y)$ 在点 $P(x_0, y_0)$ 处的性态分别为：① 极限存在 ($\lim$ 存在) ② 连续 (指二元函数整体连续)②'  关于两个变量均连续”③ 一阶偏导数存在 ($f_x', f_y'$ 存在)④ 可微 ($dz$ 存在))⑤ 一阶偏导数连续 ($f_x', f_y'$ 连续，即 $C^1$ 类)
		![[26张宇《高等数学18讲》_含目录 9.jpeg]]
    - **【考点‼️】判断可微性（在** $(x_0, y_0)$ **点）**：
        1. 用定义求出 $f_x'(x_0, y_0)$ 和 $f_y'(x_0, y_0)$。若不存在，则必不可微。PS：直接算该点的偏导数，例如 $f_x'(x_0, y_0)$ 可以先算 $f_x'(x, y_0)$  不用算 $f_x'(x, y)$（即可以先代入点 $y=y_0$），或者用定义 $f_x'(x_0, y_0)=\lim_{x \to x_0} \frac{f(x, y_0) - f(x_0, y_0)}{x - x_0}$
        2. 计算极限 $\lim_{(\Delta x, \Delta y) \to (0,0)} \frac{\Delta z - (f_x'(x_0, y_0)\Delta x + f_y'(x_0, y_0)\Delta y)}{\sqrt{(\Delta x)^2 + (\Delta y)^2}}=\frac{\Delta z - dz}{\sqrt{(\Delta x)^2 + (\Delta y)^2}}$。其中 $\Delta z = f(x_0+\Delta x, y_0+\Delta y) - f(x_0, y_0)$ 但是一般不要写 $dz$ ，因为写了 $dz$ 就等于默认全微分存在，所以第二个等号只是为了理解，严谨来说判断可微不要写
        3. 若极限等于 0，则 **可微**；若极限不等于 0 或不存在，则 **不可微**。（例13.8）
## 二、‼️多元函数微分法则

6. **链式求导规则 (Chain Rule)**
    - **【考点】核心**：画出变量间的复合结构图，将所有“路径”加起来。
    - 复合一元函数：设 $y = f(u)$ 且 $u = g(x)$
	    - $\frac{dy}{dx} = \frac{d\{f[g(x)]\}}{dx} = \frac{d\{f[g(x)]\}}{d[g(x)]} \cdot \frac{d[g(x)]}{dx}$
    - **复合二元函数形式一**：设 $z = f(u,v)$，$u = \varphi(x,y)$，$v = \psi(x,y)$
        - $\frac{\partial z}{\partial x} = \frac{\partial z}{\partial u} \frac{\partial u}{\partial x} + \frac{\partial z}{\partial v} \frac{\partial v}{\partial x}$
        - $\frac{\partial z}{\partial y} = \frac{\partial z}{\partial u} \frac{\partial u}{\partial y} + \frac{\partial z}{\partial v} \frac{\partial v}{\partial y}$
    - 复合二元函数形式二：设 $z = f(u,v)$，$u = \varphi(t)$，$v = \psi(t)$
	    - $\frac{\partial z}{\partial t} = \frac{\partial z}{\partial u} \frac{\partial u}{\partial t} + \frac{\partial z}{\partial v} \frac{\partial v}{\partial t}$
    - **【小众点】符号** $f_1', f_2'$：
        - $f_1'$（或 $f_2'$）表示 $f$ 对其_第1个（或第2个）位置_的变量求导，与该位置上是 $x$ 还是 $u$ 无关。（例13.9）
        - $f_x'$ 表示 $f$ 对变量 $x$ 求导。
7. **一阶全微分形式不变性**
    - **【考点】**：$dz = \frac{\partial z}{\partial u} du + \frac{\partial z}{\partial v} dv$。
    - 无论 $u,v$ 是自变量，还是中间变量（例如 $u=u(x,y), v=v(x,y)$），全微分的形式保持不变。
    - **应用**：用于**隐函数求导**非常方便。
    - 证明：
8. **隐函数存在定理（公式法）**
    - **【考点】一个二元方程** $F(x,y) = 0$ **确定**一元函数 $y=f(x)$：
        - 若 $F_y'(x,y) \ne 0$，则 $\frac{dy}{dx} = - \frac{F_x'(x,y)}{F_y'(x,y)}$。
    - **【考点】一个三元方程** $F(x,y,z) = 0$ **确定**二元函数 $z=f(x,y)$：
        - 若 $F_z'(x,y,z) \ne 0$，则：
        - $\frac{\partial z}{\partial x} = - \frac{F_x'(x,y,z)}{F_z'(x,y,z)}$
        - $\frac{\partial z}{\partial y} = - \frac{F_y'(x,y,z)}{F_z'(x,y,z)}$
    - **【小众点】存在性条件**：
        - $F_y' \ne 0$ (或 $F_z' \ne 0$) 是隐函数**存在且可导**的**充分条件**，但**非必要条件**。
        - （例13.14）反例：$F(x,y) = x^3 - xy = 0$，在 $(0,0)$ 点 $F_y' = -x = 0$，但仍可确定 $y=x^2$。
	- 【小众点】**隐函数方程组**求导：设 $\begin{cases} F(x,y,z) = 0, \\ G(x,y,z) = 0, \end{cases}$ 当满足 $\frac{\partial(F,G)}{\partial(y,z)} = \begin{vmatrix} \frac{\partial F}{\partial y} & \frac{\partial F}{\partial z} \\ \frac{\partial G}{\partial y} & \frac{\partial G}{\partial z} \end{vmatrix} \neq 0$ 时，可确定 $\begin{cases} y = y(x), \\ z = z(x). \end{cases}$ 其复合结构图如图所示：![[Pasted image 20251116172426.png|100]] 且有 $\frac{dy}{dx} = -\frac{\frac{\partial(F,G)}{\partial(x,z)}}{\frac{\partial(F,G)}{\partial(y,z)}} = -\frac{\begin{vmatrix} \frac{\partial F}{\partial x} & \frac{\partial F}{\partial z} \\ \frac{\partial G}{\partial x} & \frac{\partial G}{\partial z} \end{vmatrix}}{\begin{vmatrix} \frac{\partial F}{\partial y} & \frac{\partial F}{\partial z} \\ \frac{\partial G}{\partial y} & \frac{\partial G}{\partial z} \end{vmatrix}}, \quad \frac{dz}{dx} = -\frac{\frac{\partial(F,G)}{\partial(y,x)}}{\frac{\partial(F,G)}{\partial(y,z)}} = -\frac{\begin{vmatrix} \frac{\partial F}{\partial y} & \frac{\partial F}{\partial x} \\ \frac{\partial G}{\partial y} & \frac{\partial G}{\partial x} \end{vmatrix}}{\begin{vmatrix} \frac{\partial F}{\partial y} & \frac{\partial F}{\partial z} \\ \frac{\partial G}{\partial y} & \frac{\partial G}{\partial z} \end{vmatrix}}$ 两个三元函数方程确定两个一元函数方程组
9. 全微分公式大观
	1. $xdx + ydy = d\left(\frac{x^2+y^2}{2}\right);$
	2. $xdx - ydy = d\left(\frac{x^2-y^2}{2}\right);$
	3. $ydx + xdy = d(xy);$
	4. $\frac{ydx + xdy}{xy} = d(\ln|xy|);$
	5. $\frac{xdx + ydy}{x^2 + y^2} = d\left[\frac{1}{2}\ln(x^2+y^2)\right];$
	6. $\frac{xdx - ydy}{x^2 - y^2} = d\left[\frac{1}{2}\ln|x^2-y^2|\right];$
	7. $\frac{xdy - ydx}{x^2} = d\left(\frac{y}{x}\right);$
	8. $\frac{ydx - xdy}{y^2} = d\left(\frac{x}{y}\right);$
	9. $\frac{xdy - ydx}{x^2 + y^2} = d\left(\arctan \frac{y}{x}\right);$
	10. $\frac{ydx - xdy}{x^2 + y^2} = d\left(\arctan \frac{x}{y}\right);$
	11. $\frac{ydx - xdy}{x^2 - y^2} = d\left(\frac{1}{2}\ln\left|\frac{x-y}{x+y}\right|\right);$
	12. $\frac{xdy - ydx}{x^2 - y^2} = d\left(\frac{1}{2}\ln\left|\frac{x+y}{x-y}\right|\right);$
	13. $\frac{xdx + ydy}{(x^2 + y^2)^2} = d\left(-\frac{1}{2(x^2+y^2)}\right);$
	14. $\frac{xdx - ydy}{(x^2 - y^2)^2} = d\left(-\frac{1}{2(x^2-y^2)}\right);$
	15. $\frac{xdx + ydy}{1 + (x^2 + y^2)^2} = d\left[\frac{1}{2}\arctan(x^2+y^2)\right];$$\frac{xdx - ydy}{1 + (x^2 - y^2)^2} = d\left[\frac{1}{2}\arctan(x^2-y^2)\right];$
10. 【小众点】**二元函数的拉格朗日定理**
    - **【小众点】**：若 $f(x,y)$ 在一个区域 D（连通开集）上 $\frac{\partial f}{\partial x} \equiv 0$ 且 $\frac{\partial f}{\partial y} \equiv 0$，则 $f(x,y) \equiv C$（常数）。
    - **【小众点】注意**：仅仅 $\frac{\partial f}{\partial x} \equiv 0$ **不能**推出 $f(x,y) = g(y)$（即 $f$ 与 $x$ 无关），这需要区域 D 在 $x$ 方向上是“连通”的（例如凸区域）。（讲义P21给出了反例）
## 三、多元函数的极值与最值

10. **概念**
    
    - **极值（局部）**：在某点的一个邻域内最大/最小。
        
    - **最值（全局）**：在整个定义域内最大/最小。
        
    - **【小众点】**：$f(x,y)$ 在 $(x_0, y_0)$ 取极值 $\implies$ $f(x, y_0)$ 在 $x_0$ 处取极值 且 $f(x_0, y)$ 在 $y_0$ 处取极值。
        
    - **反之不成立**。（例13.17）
        
11. **无条件极值**
    
    - **【考点】必要条件**：若 $f$ 在 $(x_0, y_0)$ 处可导且取极值，则 $f_x'(x_0, y_0) = 0$ 且 $f_y'(x_0, y_0) = 0$。
        
    - **可疑点（驻点）**：使 $f_x' = 0$ 且 $f_y' = 0$ 的点。
        
    - **可疑点（奇异点）**：偏导数不存在的点（如 $z=\sqrt{x^2+y^2}$ 在(0,0)）。
        
    - **【考点】充分条件（二阶判别法）**：
        
        1. 设 $(x_0, y_0)$ 为驻点。
            
        2. 记 $A = f_{xx}''(x_0, y_0)$，$B = f_{xy}''(x_0, y_0)$，$C = f_{yy}''(x_0, y_0)$。
            
        3. 计算 $\Delta = AC - B^2$。
            
        4. $\Delta > 0$：有极值。
            
            - $A < 0 \implies$ 极大值。
                
            - $A > 0 \implies$ 极小值。
                
        5. $\Delta < 0$：**无**极值（为鞍点）。
            
        6. $\Delta = 0$：方法失效，需另行判断。
            
12. **条件最值与拉格朗日乘数法**
    
    - **【考点】问题**：求目标函数 $u = f(x,y,z)$ 在约束条件 $\varphi(x,y,z) = 0$ 和 $\psi(x,y,z) = 0$ 下的最值。
        
    - **【考点】方法**：
        
        1. 构造**拉格朗日函数**： $L(x,y,z,\lambda,\mu) = f(x,y,z) + \lambda\varphi(x,y,z) + \mu\psi(x,y,z)$
            
        2. 解方程组：
            
            - $L_x' = f_x' + \lambda\varphi_x' + \mu\psi_x' = 0$
                
            - $L_y' = f_y' + \lambda\varphi_y' + \mu\psi_y' = 0$
                
            - $L_z' = f_z' + \lambda\varphi_z' + \mu\psi_z' = 0$
                
            - $L_\lambda' = \varphi(x,y,z) = 0$
                
            - $L_\mu' = \psi(x,y,z) = 0$
                
        3. 解出的 $(x,y,z)$ 即为所有可疑点。
            
        4. 根据问题实际意义或比较各点函数值，确定最值。
            
    - **【小众点】边界检查**：如果约束条件（如 $\varphi=0$）本身不是封闭的（例如一个线段而不是一个圆周），则必须额外检查约束条件的**端点**。（例13.20）
        
13. **【小众点】最远（近）点的垂线原理**
    
    - **原理**：从点 Q 到光滑曲线 $\Gamma$ 的最远/最近点 P，连线 QP 必垂直于曲线 $\Gamma$ 在 P 点的切线。
        
    - **应用**：可作为几何辅助，快速建立方程，（例13.21）。
        
14. **有界闭区域上连续函数的最值问题**
    
    - **【考点】定理**：有界闭区域 $D$ 上的连续函数 $f(x,y)$ 必定存在最大值和最小值。
        
    - **【考点】求解步骤**：
        
        1. **内部**：求 $D$ 内部的所有可疑点（驻点 $f_x'=0, f_y'=0$ 和奇异点）。
            
        2. **边界**：求 $f(x,y)$ 在 $D$ 的边界上的最值。
            
            - 方法一：用拉格朗日乘数法（将边界视为约束条件）。
                
            - 方法二：参数化边界（例如 $x=\cos\theta, y=\sin\theta$），将 $f(x,y)$ 转化为一元函数，再求一元函数的最值。
                
        3. **比较**：比较所有内部可疑点和边界最值点的函数值，最大的为最大值，最小的为最小值。