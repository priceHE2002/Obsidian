# 2026-09-10 工作日志

## 计组强化 P2 讲义 → Obsidian 笔记（第三章 存储系统）

**背景/需求**
- 用户要求把「计组强化P2_存储系统大串讲（计组×操作系统）_课前打印.pdf」中缺少的内容补上并整理到 Obsidian。
- 澄清后确认：缺口是 `0_基础考点讲解/3_第三章 存储系统` 里 **3.6 虚拟存储器整节缺失**；且强化讲义 P2 只有 15 张图、无文字细节。

**讲义结构（已精读）**
- 17 页扫描图，无文字层；图1–图15 连续，第 16/17 页是重复的空白页框模板。
- 统一假设：36 位系统 / 按字节编址 / 页 4KB / 虚拟地址 36bit（虚页号 24bit）/ 物理地址 32bit（页框号 20bit）/ Cache 块 64B。
- 图1–图9 = 虚拟地址→物理地址（操作系统视角）；图10–图15 = 物理地址→Cache（计组视角）。
- 讲义已确认的关键数值：页表始址 `98521H`、页框号 `AE866H`、二级页表 `985H → 11111H → 211H`、物理地址 `AE866996H`；
  4 路组相联 TLB 示例虚页号 `8FFBFDH` → 组号 5、标记 21bit。

**产出（新增目录 `我的笔记/第三章 存储系统/`）**
- `00_存储系统总览 MOC.md`：章节结构、缺口对照表、讲义图索引、统一假设与地址位段计算。
- `01_虚拟存储器与分页存储管理（基础细节）.md`
- `02_地址转换全流程详解（讲义图1–图9）.md`：图2 的 ①~⑨ 步逐条文字化 + 逐图拆解 + 访存次数表。
- `03_TLB 详解（全相联与组相联、有效位与进程切换）.md`
- `04_通过物理地址访问 Cache 详解（讲义图10–图15）.md`：三种映射地址划分 + 替换算法/写策略/平均访问时间补充。
- `05_虚拟存储器 vs Cache 对比 & 访存次数总结.md`
- `06_段式与段页式虚拟存储器.md`
- `07_408 题型模板与易错点速查.md`
- `assets/`：讲义 15 张图（PyMuPDF 105dpi / JPEG q82 导出），共 6.9MB。

**结论/约定**
- 用户偏好：按 **408 考研标准**补齐细节；要求 **新建笔记子目录** 存放（不再沿用旧的 `XX_Obsidian笔记.md` 平铺命名）。
- 目录约定：`03_计算机系统/1_计算机组成原理/我的笔记/<章名>/`，章内用 `NN_标题.md` + `assets/` 子目录。

**工具环境**
- 本机无 pdftotext/pdfinfo；已在 `/Users/heyuhang/.workbuddy/binaries/python/envs/default` 建 venv 并装 PyMuPDF（`import pymupdf`，勿用 `fitz`）。

## 修订（同日 21:56）：8 篇拆分为 1 篇合并笔记

- 用户要求"把这些放在同一个笔记内"→ **偏好是"一章一篇完整笔记"，不是一章一套拆分笔记**。
- 合并为 `我的笔记/第三章 存储系统/第三章 存储系统（完整笔记）.md`（1414 行 / 36K 字符），
  结构 = 1 个 H1 标题 + 8 个 H1 大节（0 总览 / 1 虚存基础 / 2 地址转换 / 3 TLB / 4 Cache 访问 /
  5 对比与访存次数 / 6 段式段页式 / 7 模板与易错点），每节内用 H2 细分。
- 16 处图片嵌入全部指向 `assets/`，校验通过；无残留跨篇 wikilink。
- 原 8 个 .md 已移入 **macOS 废纸篓**（`~/.Trash/`），可从废纸篓找回。
- `assets/` 目录保留不变。

## 修订（同日，第三轮）：对照基础班讲义 + 大纲，把 3.1–3.4 补成完整内容

- 用户反馈"还是缺东西，请你对照 `0_基础考点讲解` 中还有 408 计算机组成原理大纲的来"。
- 定位真实缺口：合并版只把 **3.1 存储系统基本概念 / 3.2 主存储器 / 3.3 主存与 CPU 的连接 /
  3.4 外部存储器** 压缩成速查表，且 **3.6 虚拟存储器** 基础班根本没有对应讲义。
- 已提取 `0_基础考点讲解/3_第三章 存储系统/` 全部 **12 个讲义 PDF**（均有文字层，区别于强化讲义 P2 的纯扫描图），
  并对照 `计算机组成原理总览（含新大纲考点）.pdf` 第三章考点清单，逐条重写：
  - 3.1：五维分类、层次化结构、性能指标（存取时间/周期/带宽）、存储字长与寻址
  - 3.2：半导体存储器组成、SRAM vs DRAM 对比、DRAM 刷新三策略、行列地址复用、ROM 系列、双端口 RAM（大纲已删）、多模块存储器（低位交叉 $t=T+(n-1)r$、$m\ge T/r$）
  - 3.3：位扩展 / 字扩展 / 字位同时扩展、线选法 vs 译码片选法、解题模板
  - 3.4：磁盘组成与磁记录、性能指标（平均存取时间、数据传输率 $D_r=rN$）、地址与工作过程、RAID0–5、SSD 磨损均衡与寿命
  - 3.5 / 3.6：保留原详细内容，3.5 增加多级 Cache（大纲新增），3.6 标注"基础班缺失，由教材+大纲+讲义补齐"
- 最终文件：`我的笔记/第三章 存储系统/第三章 存储系统（完整笔记）.md`
  （**1838 行 / 47826 字符**；H1 结构 = 0 导航 / 3.1 / 3.2 / 3.3 / 3.4 / 3.5 / 3.6 / 7 对比专题 / 8 模板与易错点）。
- 校验：15 处图片嵌入（Obsidian 裸文件名 wikilink）在 vault 内全部唯一可解析、无缺失无歧义；
  38 个大纲关键词全覆盖；`<!--PARTn-->` 分段占位标记已全部清除。
- 备忘：Obsidian 图片嵌入用**裸文件名** `![[xx.jpg]]` 即可（靠 vault 内唯一文件名解析），
  本 vault 内这些讲义图名唯一，无需加 `assets/` 前缀。

## 修订（同日 22:06）：修 Obsidian tags 类型警告

- 现象：笔记属性面板 `tags` 报 ⚠，值显示为 `[408,"计算机组成原理",...]`。
- 原因：YAML 里 `- 408` 被解析成**数字**，Obsidian 的 `tags` 属性只接受**字符串**，故类型不匹配。
- 修复：`- 408` → `- "408"`（加引号强制为字符串）。
- **约定：frontmatter 里所有 tag 一律写字符串；纯数字 tag 必须加引号（如 `"408"`），否则 Obsidian 报类型警告。**
- 顺带全 vault 扫描：另有两处同类隐患 `Orca.md`（tag `2026`），位于 `07_DL基础及论文进阶/…` 与其 worktree 副本，非本次任务范围，仅记录未改动。

## 修订（同日 22:12）：优化笔记编号

- 用户："请你优化一下编号"。
- 原问题：H1 层级**混用两套编号** —— `# 0.`（level-1）/ `# 3.1–3.6`（level-2）/ `# 7.` `# 8.`（level-1），且 3.6 之后**跳号**到 7、8。
- 改法：统一为 `3.x` 单链 —— `# 3.0 导读` + `# 3.1–3.6`（原样）+ `# 3.7 专题对比` + `# 3.8 专题模板` + `# 3.9 附录讲义清单`；
  H2 相应变为 `3.0.1–3.0.3` / `3.7.1–3.7.4` / `3.8.1–3.8.5`；原 `### 8.1.1–8.1.6` 改为 `### ①②③④⑤⑥`（与 3.5.1 风格一致）；`## 附：…` 提升为 H1 `# 3.9 附录`。
- 同步 4 处正文交叉引用：`第 7 节→第 3.7 节`、`第 8 节→第 3.8 节`、`第 0.3 节→第 3.0.3 节`、`第 7.4 节→第 3.7.4 节`。
- 校验：22 条标题全部命中；无遗留 0./7./8. 编号与 `8.1.x` 标题；15 处图片嵌入仍全部可解析；1838 行不变。
- 备份：改前版本存于 `/tmp/bak_before_renumber.md`（临时，可删）。

## 修订（同日 22:27）：排版优化

- 用户："请你再优化一下排版，使其更美观"。
- 先跑排版体检，定位问题后逐项修（备份 `/tmp/bak_before_format.md`）：
  1. **8 处 callout 把整句当标题** → 改为「短标题 + 换行 + 正文」（如 `> [!warning] 每次被访问的主存块…` → `> [!note] 补充` + 正文）。
  2. 篇首补一行**标注约定图例**（important/danger/warning/tip/note/info 各代表什么）。
  3. 5 处 `📎 对应基础班讲义：…` 改为引用块 `> 📎 **对应讲义**：…`，与正文视觉分层。
  4. **公式前补空行**（`**典型计算题**…` 后直接接 `$$`）。
  5. **列表内嵌块公式**改为行内公式（段式虚实地址变换那条），避免 Obsidian 渲染不稳。
  6. 中英间距：`Cache块` → `Cache 块`（4 处）。
  7. **图号间距统一**：`图1`→`图 1`、`图10–图15`→`图 10～图 15`（16 行；**跳过 `![[…]]` 嵌入行**，文件名不动）。
- 校验：15 处图片嵌入仍全部可解析；`SRAM和DRAM.pdf`、`Cache和主存的映射方式.pdf` 等**文件名零改动**；
  1847 行 / 48061 字符；全部 callout 标题均已合规。
- 注意：用户已自行把 frontmatter 的 `"408"` tag 删掉（现 tags 只剩 5 项），我未恢复。

## 安装 Obsidian CSS Snippet：Quiet Notes（同日 22:33）

- 用户要求安装 `https://github.com/andrewchen0623/obsidian-quiet-notes`。
- 仓库只有 **1 个 CSS 文件** `quiet-notes.css`（3556 B / 131 行），其余是 README、LICENSE、截图。
- **安全审查**：无 `url()`、无 `@import`、无 `javascript:` / `expression()`、无远程 http(s) 与 `data:` URI
  → 纯排版规则（正文行高 1.8、段距 1.7em、标题层级、表格、高亮、引用块、浅/深色变量），**安全**。
  设计上把布局规则限定在 `.markdown-rendered` / `.mod-cm6`，不动 Obsidian 界面组件。
- 已装到 `/Users/heyuhang/Documents/Obsidian Vault/.obsidian/snippets/quiet-notes.css`，SHA256 与源一致。
- **重要caveat**：Obsidian 当时**正在运行**（PID 95348），所以**没有**直接改 `.obsidian/appearance.json`——
  Obsidian 把设置缓存在内存里，运行期改文件会被它下次保存设置时覆盖。正确做法是让用户在
  `设置 → 外观 → CSS 代码片段` 点刷新再开启开关。若确实要脚本化启用，必须先退出 Obsidian。
- vault 现有 snippets：`textbook-math.css`（已启用）、`quiet-notes.css`（待启用）。

## 配置 Obsidian 自定义字体（同日 22:35）

- 用户："中文 思源宋体 Light；外文 Anthropic-Serif Regular"。
- **现状**：两者都没装。系统原有中文字体是 方正小标宋 / 仿宋 / 楷体 / 微软雅黑。
- **来源**：
  - Anthropic Serif —— 发现 `Claude.app` 自带变量字体（`AnthropicSerif-Romans-Variable`，wght 300–800，默认 300），
    用它实例化成静态 **Regular(400) / Bold(700)**。版权归 Anthropic，仅本机个人使用。
  - 思源宋体 —— 用 Google Fonts 的 **Noto Serif SC** 变量字体（24MB，同一款字体，比 Adobe 官方 SC 包 132MB 轻很多），
    实例化成 **Light(300) / Bold(700)**。
- **落盘**：4 个 ttf 装入 `~/Library/Fonts/`（`system_profiler` 已确认登记，family 名正确）；
  变量原件移入废纸篓。
- **实现**：新建 snippet `.obsidian/snippets/custom-fonts.css`
  —— 同一 family「Quiet Serif」下 4 个 `@font-face`，用 **unicode-range** 把拉丁与 CJK 拆成互斥两组；
  中文标点（`，。“”、`）与 em/en dash、省略号划给 CJK；① ✔ 等符号不覆盖，落 fallback。
  校验：两组区间**零重叠**，样例字符归属全部正确。
  选择器用 `body.theme-*` / `body .markdown-rendered` 前缀压过默认主题与 Quiet Notes。
- **未做**：没改 `appearance.json`（Obsidian 在运行，改了会被覆盖）→ 需用户手动启用 snippet。
- 结论与坑已写入用户级 memory：`~/.workbuddy/MEMORY.md`。
- 沉淀技能：`~/.workbuddy/skills/obsidian-custom-fonts/`（含 `references/unicode-range-split.md`）。

## 配置 Obsidian 等宽字体 + 修 ASCII 图列对齐（同日 23:55）

- 用户："等宽字体请你也一样调整" → 追问后确认：**外文 Iosevka、中文仍用思源宋体 Light、顺手修图对齐**。
  （说明：**Anthropic 官方没有等宽字体**，Claude.app 里只有 `Anthropic{Sans,Serif}-*`，所以等宽西文必须换别人。）
- **关键度量（实测，别猜）**：
  - 系统等宽字体步进全是 ~0.6em：SF Mono 0.618 / Menlo 0.602 / Andale·Courier·PT Mono 0.600 → 做不出 2:1。
  - **Iosevka Fixed 每个字形步进都恰好 0.5000em**（连 `─│├└*/←→↑↓↕►‖—–…①✔` 都是），
    且 GSUB **无 `calt` / `liga`**（不会吞连字）；思源宋体 CJK 恰好 1.0em、`（）「」，：` 也 1.0em。
    → **中文 = 2 × 西文**，这正是笔记里 ASCII 图的对齐依据。
  - 思源宋体的 `—` 是 **0.881em**（非整数），所以破折号**必须**划给 Iosevka，否则网格碎掉。
- **省流下载**：Iosevka 官方整包 91MB，GitHub release 支持 **HTTP Range** → 自写 `RemoteFile`
  （**必须实现 `readinto`**，否则 `io.BufferedReader` 抛 `NotImplementedError`；直接喂 `zipfile` 别再套 BufferedReader）
  只取 3 个 ttf，**22 次请求 / 5.33MB / 71 秒**。装在 `~/Library/Fonts/IosevkaFixed-{Light,Regular,Bold}.ttf`。
- **CSS**：`custom-fonts.css` 扩成两族——正文 `Quiet Serif` + 等宽 `Quiet Mono`。
  等宽的分法**与正文相反**：`U+2500-257F` 框线 / `U+2190-21FF` 箭头 / `U+2000-206F`（`— ‖ …`）归**西文** face；
  `U+2460-24FF`（`①-⑳`）归**中文** face（Iosevka 的圈号在 14px 下只有 7px 高，看不清）。
  校验：两组 range **不相交**；笔记代码块 **314 种字符**（西文 2449 次 / 中文 779 次）**零未分配、零缺字形**。
- **框线连成整条**：量出 Iosevka 的 `│` 字形高 **1.250em**、字体自然行高 1.180em
  → 代码行 `line-height` 取 **1.22** 才能让竖线跨行接上（取 1.3 就断）。
  同时清掉 Quiet Notes 给 `.cm-line` 加的 `0.28em` 上下内距与 `0.03em` 字距（**只清 `.cm-line`，不清 `pre` 的 padding**）。
- **修图**：写脚本按同一套度量重排 5 处（层次框边框 30→32 段、两条 ↕ 行收 1 格；
  虚实地址块的 `+`/`页内地址` 对齐到 32 列；流程图右侧尺子统一到 **50 列**；Cache 分解的 `→`→16 / `←`→33；目录树 `→` 统一到 **48 列**）。
  校验：逐行算「尺子字符列号」，全部一致；**diff 只有空白/破折号变化，无任何文字改动**（备份 `/tmp/note_backup_before_align.md`）。
- **验收**：用 PIL 按浏览器行盒模型（`half + ascent` 定基线、`anchor="ls"`）渲染 4 张图，
  肉眼确认层次框是**闭合矩形**、流程图右侧尺子成一条直线、目录树 `→` 成列。验收图存
  `.workbuddy/preview/0{1..4}_*.png`。
- **注意**：`appearance.json` 里 `enabledCssSnippets` 已含 `custom-fonts`，字体项为空 → 无需手动开启；
  但**新装的 Iosevka 需重启 Obsidian** 才会进 Chromium 字体表。
- 技能已扩写：`~/.workbuddy/skills/obsidian-custom-fonts/` 新增 **`references/monospace-2to1.md`**（等宽 2:1 完整做法）。

## 三个 CSS snippet 合并为一个（同日 24:0x）

- 用户（附截图）："整合这三个为一个"。截图里 `quiet-notes` / `custom-fonts` 开着、`textbook-math` 关着。
  追问后用户选 **"一起生效"** —— 合并后 textbook-math 那段规则也要跑起来。
- 产出：`.obsidian/snippets/quiet-notes-unified.css`（**469 行**），按主题分 10 节重组：
  颜色 → @font-face(9 个) → 字体应用 → 正文排版 → 公式 → 代码/等宽 → 表格 → 其他元素 → callout → 打印。
- **消掉的 2 处冲突**（这次合并的真正工作量）：
  1. `--font-text` 在 quiet-notes（`"PingFang SC"`）和 custom-fonts（`"Quiet Serif"`）里各声明一次。
     合并后**只留字体应用块那一份**（原靠 `body .markdown-rendered` 优先级压过去，能跑但不该留）。
  2. 正文行高：quiet-notes 用 `--line-height-normal: 1.8`，textbook-math 给 `.markdown-rendered p` 写死 `1.78`。
     统一成 1.8 一个变量，阅读/编辑模式行距一致（打印里仍保留 1.65）。
- **归档方式**：原文件移到 `snippets/_archive/*.css.bak`。
  要点：**扩展名不能以 `.css` 结尾**，Obsidian 的 snippet 加载器按扩展名过滤，`.bak` 既不出现在面板又可随时改回。
- 校验：括号平衡（40/40）、`@font-face` = 9、`--font-text/interface/print/monospace` 各只剩 1 处声明、
  `line-height: 1.22` / callout 配色 / `@media print` / `HyperMD-codeblock` 规则都在。
- `appearance.json` 的 `enabledCssSnippets` 已写成 `["quiet-notes-unified"]`（仅当停机时才有意义；
  Obsidian 在运行 → 仍需用户在 `设置 → 外观 → CSS 代码片段` 里手动开开关，见下条）。
- 技能已补第 7 节「多个 snippet 合并成一个」：冲突清单（自定义属性重复声明 / 行高写死）、
  归档用 `.css.bak`、合并后要重跑自检清单。
- ⚠️ **待用户操作**：Obsidian 当前在运行（PID 15112）。需要**完全退出并重开** —
  一次解决两件事：① 新 snippet 生效；② Chromium 重载字体表（Iosevka）。
