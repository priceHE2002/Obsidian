# 2026-09-12 工作日志

## 17:20–17:40 重整 3.5.7「Cache 容量怎么算：只有 Tag 要存」

用户反馈：这节应聚焦**方法本身**，删掉混进来的具体题目（28 位地址 / Tag20·组号3·块内5 / 278 bit 那道）。

### 改写后的结构（五个小节，全部通用化）

| 小节 | 内容 |
|---|---|
| ① 为何只需额外存储 Tag | 行三段结构（有效位\|Tag\|数据）+ 组成表（有效位/Tag/数据/脏位/替换位各自的位数与适用条件）；地址三段 Tag\|Index\|Offset 对照表；ASCII 图说明 Offset 是选择信号 |
| ② Tag 位数怎么定 | $\text{Tag}=n-\text{Index}-o$，三种映射各一行（全相联 $n-o$ / 直接 $n-\log_2L-o$ / 组相联 $n-\log_2(L/N)-o$） |
| ③ 总容量怎么算（四步） | 求 $o$ → 求 Index → 求 Tag → 套总公式（行容量 × L + 每组控制位）；配「一处容易漏 / 一处容易多」callout |
| ④ 额外存储开销 | 每行开销 = Tag + 有效位 + (脏位) + (替换位)；总量 × L；数据容量与开销占比 |
| ⑤ 考场记忆版 | 两条 boxed 口诀 + 指向自测第 4 题看算例 |

**记号与 3.5.2 对齐**：物理地址 $n$ bit、块 $2^o$ B、$L$ 行、$N$ 路组相联 —— 不再自造记号。

### 具体题目去了哪

没删掉，降级成 **3.5.8 自测第 4 题**，且答案改成按 ③ 的四步走一遍
（$o=5$ → Index 3 → Tag 20 → 数据 256 → 行容量 278）。
本节末尾用 `[!tip]` 指过去，正文保持纯方法。

### 验收

- 定界符 0 处；MathJax 全篇 **236 条公式 0 报错**；编号链 3.5.1→3.5.8 单调。
- 本节内 grep `278|28 位|本题` → 无残留 ✓。
- 新 ASCII 图（去掉了"低 5 位"字样）栅格复测：`↑│└` 全在第 2.000 列，行宽均为 0.6em 整数倍 ✓。

### 新工具

`obsidian-custom-fonts/scripts/check_codeblock_grid.py` —— 量**任意**代码片段的栅格
（每行行宽是否 0.6em 整数倍 + 每个框线/箭头字符列号），弥补 verify_chromium.py 只量最长框图的不足。
/tmp 又被清了，这次直接归档进技能并在 SKILL.md §9 加了说明。

---

## 17:48–19:05 修好 vault 的 git 同步（跨目录，记录在这里备查）

### 根因链（一层套一层，共 4 个问题）

1. **钥匙串里 github.com 凭证丢失** → 插件 9-06 起提交成功、推送失败，积压 6 个提交。
   修复：用户生成 fine-grained PAT（仅 Obsidian 仓库 Contents 读写）→
   `git credential-osxkeychain store`。本机无 gh CLI、SSH key 未绑 GitHub，HTTPS+PAT 是唯一通道。
2. **目录大改组**（809 删除 + ~1800 新路径）：确认是改名非丢数据（新目录同内容文件都在）→ 直接提交。
   关键认知：git 按 blob 内容寻址，**改名不产生新 blob**，重做提交后待推量从 873MB 掉到 84 个对象。
3. **嵌入式 git 仓库**：`07_DL…/.claude/worktrees/wizardly-gould-b23ea3` 是本仓库自己的 worktree，
   被 `git add -A` 当 gitlink 收进去（推到 GitHub 是永远解析不了的空链接）。剔了两次——
   第二次是**插件自动备份提交又把它捡回来**。最终 `.gitignore` 加 `.claude/` 整目录忽略。
4. **超 100MB 大文件**：两本王道教材 PDF（208MB / 188MB）触发 GitHub GH001 拒收。
   用户拍板 **PDF 不进 git**：`.gitignore` 加 `*.pdf`，`git ls-files -z '*.pdf' | xargs -0 git rm --cached`
   出库（磁盘文件不动）。同时 `.accelerate/`（处理讲义时的临时缓存，~100MB）一并忽略。

### 最有价值的教训

- 一次推送跑了 1h39m 后报 `curl 18 Transferred a partial file`，**客户端以为失败、服务端其实成功**
  （f0fd5f5 已在远程）。排障第一步永远是 `git ls-remote origin main` 看远端真实位置，
  别信本地的 origin/main 缓存和客户端报错。
- `git rev-list --objects A..B | git cat-file --batch-check` 量"这次到底要传多少"是定位推送问题的关键；
  推送量大时按目录拆提交、推一个成一个，失败只损失当前块。
- 873MB 全在 `03_计算机系统`（用户把四门课的讲义 PDF 都挪进来了）。

### 最终状态

- 远程 = 本地 = `11761e4`，`git status --porcelain` 为 0。
- .gitignore 新增：`*.pdf`、`.accelerate/`、`.claude/`。
- PAT 已出现在对话里 → 已提醒用户过几天 revoke 换新。
