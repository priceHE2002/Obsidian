---
tags:
  - "数学大观"
  - "数一强化"
  - "模块"
章: 4
章名: "微分方程"
模块: "4.7"
模块名: "微分方程解的方程根"
题目数: 1
---
# 模块 4.7 · 微分方程解的方程根（1 题）

> [!tip] 打卡方式
> 在下方表格中点 **状态圆点** 即可切换状态，进度自动汇总到 [[数一强化进度总览]]。

```dataviewjs
const F = dv.current().file.folder.split('/').slice(0,-1).join('/');
const recs = dv.pages('"' + F + '/00_题目"').where(p => p.章 === dv.current().章 && p.模块 === dv.current().模块 && p.题号).sort(p => p.序, 'asc');
const rateMap = {'未开始':0,'已做':0.25,'已订正':0.5,'已二刷':0.75,'已掌握':1};
const ST = [['未开始','#9aa4b2'],['已做','#5b8cff'],['已订正','#34d399'],['已二刷','#2dd4bf'],['已掌握','#fbbf24']];
const root = dv.el('div','');

root.innerHTML = `
  <style>
  .msm-prog{display:flex;gap:14px;align-items:center;margin-bottom:10px}
  .msm-ring{width:96px;height:96px;border-radius:50%;background:conic-gradient(var(--acc) 0deg, transparent 0deg);flex:none}
  .msm-kpi{font-size:12px;color:var(--text-muted)}
  .msm-bar{display:flex;height:10px;border-radius:5px;overflow:hidden;border:1px solid var(--background-modifier-border)}
  table.msm-tbl{width:100%;border-collapse:collapse;font-size:13px;margin-top:6px}
  table.msm-tbl th{text-align:left;padding:6px 8px;border-bottom:2px solid var(--background-modifier-border);font-size:11px;color:var(--text-muted)}
  table.msm-tbl td{padding:5px 8px;border-bottom:1px solid var(--background-modifier-border)}
  .dot{width:15px;height:15px;border-radius:50%;border:2px solid transparent;cursor:pointer;opacity:.35;display:inline-block;margin-right:4px;vertical-align:middle;padding:0}
  .dot:hover{opacity:1;transform:scale(1.2)}
  .dot.cur{opacity:1;border-color:var(--text-normal)}
  </style>
  <div class='msm-prog'><div class='msm-ring'></div><div><div class='msm-kpi'></div></div></div>
  <table class='msm-tbl'><thead><tr><th>题号</th><th>题源</th><th>状态</th></tr></thead><tbody></tbody></table>`;
const total = recs.length;
const cnt = s => recs.where(p => p.状态 === s).length;
const counts = Object.fromEntries(ST.map(([k]) => [k, cnt(k)]));
let acc = 0; const segs=[];
for (const [k,c] of ST){ const f = total? counts[k]/total*360 : 0; if (f>0){ segs.push(c+' '+acc.toFixed(1)+'deg '+(acc+f).toFixed(1)+'deg'); acc+=f; } }
const pct = Math.round(recs.map(p => rateMap[p.状态]??0).sum()/total*100);
const ring = root.querySelector('.msm-ring'); if(segs.length) ring.style.background = 'conic-gradient(' + segs.join(',') + ')';
root.querySelector('.msm-kpi').innerHTML = '<b style="font-size:24px;font-variant-numeric:tabular-nums">'+pct+'%</b> 已完成 · 已做 '+counts['已做']+' · 已订正 '+counts['已订正']+' · 已二刷 '+counts['已二刷']+' · 已掌握 '+counts['已掌握'];
const tb = root.querySelector('tbody');
const all = recs.values;
for (const p of all){
  const tr = document.createElement('tr');
  tr.innerHTML = `<td><a data-path="${p.file.path}" style="cursor:pointer">${p['源题号']}${p.重点?' 🔥':''}</a></td><td>${p.题源??''}</td><td class="st">${ST.map(([k,c]) => `<button data-status="${k}" data-path="${p.file.path}" class="dot${p.状态===k?' cur':''}" style="background:${c}" title="${k}"></button>`).join('')}</td>`;
  tb.appendChild(tr);}
tb.querySelectorAll('a[data-path]').forEach(a => a.addEventListener('click', () => { const f = dv.app.vault.getAbstractFileByPath(a.dataset.path); if(f) dv.app.workspace.getLeaf(false).openFile(f); }));
tb.addEventListener('click', async e => { const dot = e.target.closest('button[data-status]'); if (!dot) return; const path = dot.dataset.path, s = dot.dataset.status; const file = dv.app.vault.getAbstractFileByPath(path); if(file){ await dv.app.fileManager.processFrontMatter(file, fm => { fm['状态'] = s; }); const r = all.find(x => x.file.path === path); if(r) r.状态 = s; const row = tb.querySelector(`button[data-path="${path}"][data-status]`); const rowBtns = row.parentElement.querySelectorAll('button[data-status]'); rowBtns.forEach(b => { b.classList.remove('cur'); }); dot.classList.add('cur'); } });
```