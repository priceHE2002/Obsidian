# 2026-09-12

## 两张【常见凑微分】速查卡整理入 `一元积分大观.md`
- 来源：用户两张截屏——图 2 基础版 18 条（①–⑩ 初等函数微分，⑪–⑱ 推广到 $f(x)$）、
  图 1 进阶版 16 条（乘积/商求导法则逆用，$(x\ln x)'$、$(xe^x)'$、$(x/\sqrt{1-x^2})'$ 等）。
- 落位：`高数大观/一元积分/一元积分大观.md` →「三大积分法 → 一、凑微分法」，
  在「常见凑微分形式」公式块之后插入三张表（速查卡 A-1 十行 / A-2 八行 / B 十六行），
  并各配 `[!conclusion]`／`[!tip]` 讲"本质 + 判据 + 读表口诀"。
- **核验方法（值得复用）**：落笔前把 34 条公式**逐条数值核验**——用**复步长微分**
  `f'(x) ≈ Im f(x+ih)/h`（h=1e-9）比对左右两端。比符号推导快、比中心差分准。
  唯一坑：`abs()` 非解析，复步长会返回 0，须改用解析分支或实函数中心差分。
  34 条全部通过（含图 1 第 16 条 $(x\ln x\cdots)$ 型，手算易错，实测无误）。
- 顺带修掉同文件一个渲染缺陷：表格单元里为转义 `|` 而写的 `\|`，在 MathJax 里是 `\Vert`，
  会把 $\ln|x|$ 渲染成 $\ln\|x\|$（双竖线）。**正解是 `\lvert ... \rvert`**（无裸竖线，不破表格）。
  已改 13 处（10 处旧 + 3 处新增）。**注意 `\lvert` 后紧跟字母会被当成一个控制词
  （`\lvertx`），必须补空格写 `\lvert x`。**
- 提醒用户：`线代大观/9_二次型（本质理解）.md`、`多元微分/多元微分大观.md` 各有一处
  `\|` 未改——但那里的 `\|x\|` 可能是**范数**（本意就是双竖线），故未擅动。
- MOC（`一元积分 MOC.md`）已同步：来源说明加 09-12 一行、三大积分法表下加「凑微分先查表」tip、
  核心考点速查加一条速查卡摘要。
- 结构自检：`$$` 106 个配对 ✓，19 组 `aligned` 配对 ✓，`\left`/`\right` 35:35 ✓，
  17 个表格块列数一致 ✓，无控制字符（无 LaTeX 反斜杠被吞）✓。

## 同一文件：「三大积分法」去冗余整理（939 → 911 行）
用户反馈该节内容冗余。**判据：同一事实被写两遍以上才算冗余；只是换个说法补充细节的不动。**
逐条处理后删除的重复：

| 位置 | 删掉的重复 | 保留去向 |
|---|---|---|
| 凑微分 | 「常见凑微分形式」6 行公式块 | 与 `[!conclusion] 通用形式` 合并；6 条中 5 条被速查卡 A 覆盖，**只留线性块 $\frac1a$ 那个系数例外** |
| 凑微分 | `[!tip] 反凑微分的小账本` | 4 条里 3 条＝速查卡 ①⑤⑥；并进"系数账"tip，只留 $x\,\mathrm dx=\frac12\mathrm d(x^2)$ |
| 凑微分 | `**$e^x$ 速算**` 整节（11 行） | 与速查卡 ⑯ 及原公式块三重重复；例题并入示例 callout |
| 分部积分 | `[!note] 幂函数 ➡ 0` | 终止情形表已写"典型：幂函数求导几次后为 0" |
| 分部积分 | `[!note] 其他技巧` 第 1 条 | 与终止情形表"复杂情况"行重复 |
| 分部积分 | 4 条列表法示例 | 合并为 2 条 + 1 条"对偶/降次"注（省掉 $\int xe^x$、$\int x^2\cos x$ 的独立推导） |
| 分部积分 | 两个循环积分 callout | 合并为 1 个：一条推导 + 伙伴结果 |
| 分部积分 | 「更多幂×对数」里 3 条 $x^n\ln x$ | 上方刚给的通用 $\int x^n\ln x$ 取 $n=0..3$ 即是，改为明示"上式取 $n=0,1,2,3$" |
| 换元法 | 4 处 `[!conclusion] 适用：…` | 全是分类表三列的重述，改写为标题后缀「（适用：…）」 |
| 换元法 | `**万能代换（PDF 标注 ❌）**` 整块 | 与「预备知识 → 三角公式 → 万能公式与半角代换」**逐字重复**；分类表加锚点 `[[#二、三角公式]]` + 一句"不单列" |

- 未动（判断为**非冗余**，只是风格可疑）：`[!mistake] 行列式 ❌`、`换元法与凑微分的配合`。
- 顺带改：自链接 `[[一元积分大观#三角函数组合积分]]` → `[[#三角函数组合积分]]`，与文件内其他锚点写法统一。
- 改写过的推导 **17 条全部数值复核通过**（倒代换 $-\sqrt{1+t^2}$、循环积分两条、列表法 4 条、
  $\int x^n\ln x$ 的 $n=0..3$ 代值、$\int x\arctan x$ 等）。
- 自检：911 行；`$$` 104 配对 ✓、18 组 `aligned` ✓、17 表格块列数一致 ✓、无控制字符 ✓、
  **5 个 `[[#…]]` 锚点全部命中真实标题** ✓。
- ★ **经验**：这个文件里"公式块 ↔ 速查卡表 ↔ 提示块"三者极易互相重复。
  整理时按"一个事实只留一处"重排，但**保留 PDF 原有的信息量**（系数例外、分类依据都转为标题后缀或表内注释，不是删掉）。

## 同一文件：「分部积分法」的示范全部表格化（911 → 979 行）
用户要求"把列表法的示范都改成表格"。原来的示范是一串行内箭头（`u=x^2: x^2→2x→2→0，dv=e^x: …，对角相乘 ⇒ …`），
读起来箭头串在一起。改为**每条例题一张三列列表**：`符号 | $u$ 列（上导）| $\mathrm dv$ 列（下积）`，
与本节已有的「核心操作：上导下积」描述（"第一列写 $u$…第二列写 $\mathrm dv$…"）完全对齐。共 7 张表、编号 ①–⑦：

| 组 | 例题 | 类型 |
|---|---|---|
| ①–④ | $\int x^2e^x$、$\int x^2\sin x$、$\int x^2\cos x$、$\int xe^x$ | 导积终止符（$u$ 列到 0） |
| ⑤–⑥ | $\int e^x\cos x$、$\int e^x\sin x$ | 循环型 |
| ⑦ | $\int x^n\ln x$ | 反例：$u$ 列永不归零，列表法用不上 |

- ★★ **踩到的实质错误（已修正，务必记住）**：⑤⑥ 一开始我按"逐行相乘"（第 $i$ 项 $=$ 符号$_i\times u_i\times v_i$）排了 3 行，
  以为第 3 行就是 $-I$。**这是错的**——第 3 行算出来是 $-e^x\cos x$（被积函数的相反数），不是 $-I$。
  正确规则来自 $\int u\,\mathrm dv=uv_1-u'v_2+u''v_3-\cdots$：
  - $u$ 列**归零**时：逐行相乘到 0 为止即可（①–④，公式 $=\sum(-1)^{k-1}u^{(k-1)}v_k$）。
  - $u$ 列**不归零**时：乘积只走到第 $n$ 行，**最后一项必须写成余项积分** $\int u^{(n)}v_n\,\mathrm dx$（⑤⑥⑦）。
    ⑤ 的余项 $=\int(-\cos x)e^x\mathrm dx=-I$，正是"绕回自身"的来源。
- **核验方式（这招救了命）**：不要只核结果式，要**按表格规则程序化重算**——
  写 `tab(u_col, V_col)` 生成 $\sum(\pm)u_iv_i$ 再数值求导比对被积函数（复步长微分）。
  第一版 ⑤⑥ 的"第 3 项 $=-I$"就是被这个检查逮住的（当时我另有一个 aggregate 检查写错了、
  拿导数去比函数值，误报 OK，差点放过）。
- 复核通过：①–④ 逐表重算 ✓；⑤⑥ 两行乘积和 $=2I$ ＋ 余项 $=-I$ ✓；⑦ 余项 $=\frac{1}{n+1}\int x^n\mathrm dx$ 各 $n$ ✓。
- 顺带：`**循环积分示例**` 标题改为 `**循环积分**`（"示范"字样已由 callout 标题承担）。
- 自检：979 行；`$$` 104 配对 ✓、18 组 `aligned` ✓、24 个表格块（含 callout 内嵌套表）列数全一致 ✓、无控制字符 ✓。

## ★★ 版式返工：列表法表格改成「上导下积」的上下两行（987 行）
用户打回：「**上导下积的表格你做成上下的呀，怎么做成左右的**」——
我第一版做成 `符号 | $u$ 列（上导） | $\mathrm dv$ 列（下积）` 的**竖版两列**，错了。
口诀「上导下积」的**上/下指的是行**：上行放 $u$ 及其逐次导数，下行放 $\mathrm dv$ 及其逐次积分。

**修正后的固定表式**（每条例题一张）：
```
| 步骤 | ① | ② | ③ | ④ |
|:--:|:--:|:--:|:--:|:--:|
| **上导** | x² | 2x | 2 | 0（停） |
| **下积** | eˣ | eˣ | eˣ | eˣ |
| **符号** | — | + | − | + |
```
- **下行第 ① 格写 $\mathrm dv$ 自身**（不是它的积分）——这样"对角相乘"才成立：
  上行第 $i$ 格配它**右下方**的下格，即 $u^{(i-1)}\times v_i$，符号 $+,-,+,\dots$。
- 符号行第 ① 格写 `—`：下① 是 $\mathrm dv$ 本身，只作起头不参与相乘（表下加一句解释）。
- 上行 `0` 只是"停"的标志，不参与相乘。
- ③ 的下积列顺带修正：应为 $\cos x,\ \sin x,\ -\cos x,\ -\sin x$（我第一版漏了 $\mathrm dv$ 那一格导致错位）。

**连带改**（否则前后矛盾）：`核心操作` 的"第一列/第二列"→"上行/下行"；
终止情形表的"$u$ 列某次求导变为 0"→"上行某次求导变为 0"、"越列越复杂"→"越排越复杂"。
全文已无"第一列/第二列/u 列/dv 列"残留。
`核心操作` 第 3 条把原来的"竖线积分"重构为："上行出现 0 就停；永不归零时最后一步竖着配对后积分，
即**竖线积分**，得余项 $\int u^{(n)}v_n\mathrm dx$"——保留了 PDF 的原词，同时与 ⑤⑥⑦ 三张表自洽。

**复验（按新的"上行配右下方下格"规则）**：①–④ 逐表重算 ✓；⑤⑥ 前两项 $=2I$ 原函数 ✓ 且末项竖配 $=\int u''v_2=-I$ ✓；
⑦ 首项＋余项 $=\int x^n\ln x$ 对 $n=0,1,2,3$ ✓。（中途 ⑦ 报过一次 FAIL，是我测试式把导数与函数值混写，笔记无误。）

**教训**：数学口诀里的方位词（上下/左右）是**版式硬约束**，不是修辞。先把口诀逐字落到版式上再动手，
别自作主张选"更省行数"的方向——省行数换来的是一次返工。

## 「预备知识」全部公式表格化（987 → 973 行；该节 `$$` 块降为 0）
用户给了「速查卡 A-1」的渲染截图作为**版式基准**，要求"把预备知识中的公式都整理成形如…的表格，使其更美观"。
含义 = 把散落的 `$$` 公式块改成速查表：`序号 | 名称 | 公式 | 用途` 三列，首列居中 `|:--:|`。

**一、恒等变形 → 一张 7 行表**（变形 | 公式 | 积分中的用处）
  立方和/差 · 平方差 · 完全平方 · 完全立方 · 除法拆项 · 换底 · 对数运算。
  原「[!note] 对数公式三件套」并入第 ⑦ 行；原「[!note] 恒等变形小结」降级为表前一句导语（四大类：因式分解·展开·拆项·换底）——
  表格的"用处"列已覆盖小结内容，留成 callout 就是重复。

**二、三角公式 → 重排为 7 张表**（原顺序是 特殊值→和差→倍角→化积→正割→万能→诱导→反三角；按逻辑重排）：
| 新编号 | 内容 | 版式 |
|---|---|---|
| （1）同角关系 | 商数/倒数/平方/平方恒等式 | 名称·公式·用处 |
| （2）特殊角 | 保留原网格表 | 角 × 函数 |
| （3）和差·倍角·降幂·辅助角 | 四条 | 名称·公式·用处 |
| （4）和差化积 | 一行一条，共 4 行 | 来源·化积结果·备注（④ 标"唯一的负号"） |
| （5）万能代换 | 6 个量 | **两栏并排**（原量·用 t 表示 ｜ 原量·用 t 表示） |
| （6）诱导公式 | 原 3 条 inline 式 → **矩阵表** | 角 × sin/cos/tan ✅ 这个提升最明显 |
| （7）反三角 | 互余 / 反函数还原 | 名称·公式 |

- 被并入表格后删掉的 callout：`[!conclusion] 降幂公式的积分意义`、`[!tip] 正割余割的平方恒等式`、`[!note] 对数公式三件套`；
  **保留** `[!note] 万能代换用途`、`[!tip] 诱导公式的积分意义`（这两条是"怎么用"，不是公式本身）。
- 表格单元里放不下 `aligned`，二倍角的 $\cos 2a$ 链条压成单行 `cos²a−sin²a=2cos²a−1=1−2sin²a`（速查卡里这样是惯例）。
- `\text{arccot}\,x` 顺手改成 `\operatorname{arccot}x`。
- **核验**：和差化积 4 条、辅助角 3 组系数、诱导公式 3×3=9 条、半角代换 sin/cos/tan/cot 四式、平方关系/平方恒等式 ——
  全部数值验过 ✓。（中途一次 FAIL 又是测试式写错：验 $\tan(2\arctan t)$ 而非 $\tan(\arctan t)$，笔记无误。）
- 自检：973 行；该节 `$$` 块 **0** 个 ✓；31 个表格块列数全一致 ✓；`aligned` 14/14、`\left`/`\right` 39/39、无控制字符 ✓。
- ★ **经验**：这类"公式很多但彼此独立"的小节，**表格化的收益远大于去冗余**——
  同样的信息量，行数反而降了 14 行，而且用途列把"背了干什么用"直接摊在眼前。

## ★ 全文整理：按同一套策略过完「一元积分大观」整篇（973 → 871 行）
用户："根据刚刚你调整的策略，完善并调整整个一元积分大观"。即：**去冗余 + 表格化 + 版式统一** 三件事并行。
用任务清单跟踪，共 6 个子任务全部完成。逐节动作：

| 节 | 动作 |
|---|---|
| 一、不定积分 | `[!conclusion]` 与独立 `$$` 式**重复**，合并进 callout；「基本运算性质」3 条 → **性质表**（性质·公式·说明） |
| 二、导积关系 | 「基本积分表」callout 与「记忆方法」tip **重复**（都讲"倒过来记"）→ 只留 tip（含 $\int\tan x$ 推导）；三张表加序号 ①②③、首列居中、`\frac`→`\dfrac` |
| 定积分独有方法 | 节首加**6 行导航表**（方法·什么时候想起它·一招）；奇偶性：结论里的两条 bullet 是公式复述→删；平移变换：两条结论 → **表**，口诀「区间减函数加/区间加函数减」保留为 tip；**区间再现的两个推论 A/B → 表**（推论·结论·怎么来的）；Wallis 值表 8 行 → **6 行**（sin/cos 相同的合并、按 $n$ 排序），`\frac`→`\dfrac`；「推论1/推论2」两个 tip 合并成一条 |
| 特殊反常积分 | **删掉文末「四、速查表」**（6 行全部与前面重复）；节首加一句四类导语；贝塔函数的**散文式「核心性质」→ 性质表**（对称性/与伽马关系/特殊值/换元形式），与伽马那张性质表同款式；两张表 `\frac`→`\dfrac` |
| 化简手法 | 六个平行小节（拆项/提项/同除/同乘/配方/三角化简）→ **手法总表**（手法·什么时候用·怎么操作）+ **「变形速查表」17 行**（原始形式·变形后·用了哪一手）+ 配方两方向 tip + 配方例题 + 三角化简导流 note。原六个 `### 一~六` 子标题全部取消 |
| 三角函数组合积分 | 「三角相关技巧」(1)~(5) → **一张技巧表**（技巧·公式·什么时候用），两条配套凑微分独立成 tip；「三角高次幂」两块 → **一张 4 行表**（类型·次数·怎么办）；「根号下加比减」两条 → 表 |
| 有理函数积分 | 总流程表加"例"列（原来两条 `[!example]` 算的是**同一个积分** $\frac{x^2}{x+1}$，合并进表）；「真分式的处理」情况1/2 → **一张 2 行表**（情况·判断·处理），原 3 条 callout 折入；待定系数法 4 步 → 表 |
| 变限积分与分段函数 | 方法1/方法2 → **一张 2 行表**（方法·什么时候用·怎么做）；分段函数「三步法」→ 表 |

- **顺手修正一处原文错误**：面积例里的 $\int_{-\infty}^{a}\sqrt{a^2-x^2}\,\mathrm{d}x=\frac{\pi a^2}{4}$ ——
  $\sqrt{a^2-x^2}$ 只在 $[-a,a]$ 上非零，这个积分就等于 $\int_{-a}^{a}$；且 $\frac{\pi a^2}{4}$ 对不上（那是四分之一圆）。
  原意应是 $\int_{0}^{a}=\frac{\pi a^{2}}{4}$。已改成上下两个式子并数值验证（梯形积分，$a=1,2,1.5$ 全对上）。
- **两个带数学的标题改回纯文本**（Obsidian 标题不渲染 MathJax）：
  `### 五、Wallis 公式（sin、cos 高次幂定积分）`、`### 一、指数零到无穷积分（结果是阶乘）`。
- **判定原则（这次执行得比较顺）**：callout 若只是把表格里那行公式**换种说法再念一遍** → 删；
  若在讲**怎么用 / 为什么 / 易错点** → 留。例题一律保留。
- **复验**：面积 $\int_{-a}^{a}\sqrt{a^2-x^2}=\frac{\pi a^2}{2}$、$\int_0^a=\frac{\pi a^2}{4}$ 数值积分 ✓；
  Wallis 值表 $n=2,3,4,6$ 及 $\int_0^\pi\cos^6$、$\int_0^{2\pi}\sin^6$ ✓；混合留数法三例的系数 $A=-\frac1{10},C_1=C_3=1,B=-2,C=0$ ✓；
  $\frac{x^2}{x+1}=x-1+\frac1{x+1}$、$\frac{x^3}{x+1}=x^2-x+1-\frac1{x+1}$ ✓。
- 自检：871 行（973→871，净 −102）；**44 个表格块**列数全一致；`$$` 64 配对、`aligned` 14/14、
  `\left`/`\right` 42/42、无控制字符、6 个锚点全命中；标题层级 `#`/`##`/`###` 三层规整。
- ★ **经验**：一篇文章里"同一件事重复说两遍"的高发位置 = **节末的 recap 表**、
  **并列小节的 callout 开头**、**`$$` 独立公式块紧跟着的 callout**。这三处优先查。

## 并入讲义《不定积分·有理函数·全体原函数》（871 → 947 行）
来源：用户拖入的 PDF（小红书"星河洒分高等数学答疑"，8 页，2026-07）。
**读取过程踩了两个坑**：① 微信容器目录 `~/Library/Containers/com.tencent.xinWeChat/...` 被 macOS TCC 挡住，
沙箱/非沙箱/Finder 代拷全都 Operation not permitted —— **用户把文件挪到桌面后直接可读**（下次直接让用户挪）；
② 该 PDF 是**自定义字体编码**，pypdf 抽出的中文全是乱码、但 LaTeX 数学式基本可读 →
**用 PyMuPDF 把 8 页渲染成 PNG 再用多模态读**，一次到位（venv 里 `pip install pymupdf`，`page.get_pixmap(dpi=150)`）。

**讲义内容 → 落位**：
- 有理函数 7 例 → `有理函数积分` 新增「### 五、配套例题：三次与四次有理函数家族」：
  三大方法对号表（直接凑微分/部分分式分解/倒代换拆项配方）+ 例题总表（积分·关键一步·结果）+ 例 4 完整推导（`$\boxed{}$`）。
- $\int\frac{dx}{1+x^4}$ 的 $x=0$ 陷阱 → `[!mistake]`（同除 $x^2$ 在 0 不合法；教材默认 $x\ne0$；要 $\mathbb R$ 上全体原函数需拼接），
  并补了 $\mathbb R$ 上的连续原函数（$\pm\frac{\pi}{4\sqrt2}$ 相位补偿）。
- 分段求全体原函数 → `变限积分与分段函数`：
  原「三步法」升级为讲义的**四步**（新增 ①确认连续性）；
  新增「### 三、换元失效点的拼接」+ 例 9（660 第 480 题）+ **两题对比表**（例 8 表达式分段 vs 例 9 换元失效）。
- MOC 的来源说明与「核心考点速查」同步。

**核验（全部通过）**：例 1/2/3/5/6 逐条复步长微分 ✓（例 3 在 $x<1$、$x>1$ 两支分别验，例 5 三段去绝对值）；
例 4 在 $x>0$、$x<0$ 两支 ✓；单侧极限 $\arctan\frac{x^2-1}{\sqrt2x}\to\mp\frac\pi2$ ✓、对数项→0 ✓、拼接常数闭合 ✓；
例 8 两段求导 + $C_1=C_2-1$ ✓；例 9 $F=\frac{1}{\sqrt2}\arctan(\sqrt2\tan x)$ 求导 $=\frac{1}{1+\sin^2x}$ ✓、
单侧极限 $\pm\frac{\pi}{2\sqrt2}$ ✓、取 $F(\frac\pi2)=0$ 后闭合 ✓。

**★ 讲义本身漏了一步，我补了并验证**：例 7（$\int\frac{x}{1-x^3}dx$）讲义只写"积分得相应对数与反正切组合（形式同例 6，符号调整）"。
推导时先踩了一个坑——把分解式 $\frac13(\frac{1}{1-x}+\frac{x-1}{1+x+x^2})$ 的分子待定成了 $x+2$（那是**例 3** 的），
因为我把"通分后分子应等于 $x$"错设成"等于 1"。正确结果是
$\frac16\ln\frac{1+x+x^2}{(1-x)^2}-\frac{1}{\sqrt3}\arctan\frac{2x+1}{\sqrt3}+C$
（注意反正切系数是 $\frac{1}{\sqrt3}$，不是 $\frac{1}{3\sqrt3}$ —— 第一版又错了一次，被数值核验抓出来）。
**教训：合并/改编推导时，"通分后分子等于谁"要看原积分的分子，别顺着上一题抄。**

自检：947 行；47 个表格块列数全一致；`$$` 78 配对、`aligned` 14/14、`cases` 9/9、`\left/\right` 54/54、无控制字符、5 锚点全命中。
**又踩了一次"标题带数学"**：新加的 `### 五、配套例题：$\dfrac{1}{1\pm x^3}$…` —— 上次刚立的规矩，这次差点又犯，已改纯文本。






