# 2026-09-18

## 微分方程笔记：平铺 40 节 → 六大板块 + MOC（重排 / 去重 / 补缺 / 改名）

**用户诉求**：「进一步整理微分方程方程的笔记，使其更有逻辑」。经确认选定**重排 + 建 MOC + 改名**，
内容层面**只重排去重补缺、不加例题**。

**落地**（`高数大观/微分方程/`）：
- `git mv 微分方程知识点_Obsidian详细笔记.md → 微分方程大观.md`（1241 行），旧名保留在 aliases；
- 新建 `微分方程 MOC.md`（88 行），对齐「一元积分 / 函数极限 / 多元微分」的 MOC 体例；
- 三个兄弟 MOC 的「同库其他专题」互链各补一行 `[[微分方程 MOC]]`，形成闭环。

**新结构**：总览（主线 + 知识地图 + 解题总顺序八步）→ 一、考试大纲与复习定位 →
二、基本概念与解的结构 → 三、一阶微分方程（3.1 判型树 / 3.2 可分离 / 3.3 一阶线性 /
3.4 Bernoulli / 3.5 三种换元 / 3.6 Mdx+Ndy=0 四条出路）→ 四、高阶微分方程 →
五、综合与应用 → 六、速查与易混 → 七、公式默写自测 → 附录：待补充清单 → 相关链接。

**四处动刀**：
1. 判型信息由 **5 处收拢为 2 极**（原来散在判型总流程、Mdx+Ndy 判结构、次数比较法、两张速查表、考场顺序）；
2. 删重复：原 §7 与 §9 的"整体换元"、原 §20 与 §34 的"待定系数表"各重复一遍；
3. 接链：Bernoulli 前移到一阶线性之后；"线性方程解的性质与结构"从原 §4 上提为独立板块「二」，
   与下游「4.3 常系数非齐次」接成"结构 → 待定系数"一条线；
4. 自测合并：原 §37 默写题与 §38 答案相隔约 200 行，改成「七、公式默写自测」的
   `[!question]` + 嵌套 `[!success]-` 折叠块，点开即见答案；原 §39 背诵版收进 6.5，原 §40 待补充清单移文末附录。

**新增/补缺**（都标 `> [!info] 补充说明`）：
- 3.6 **出路一：可分离**（原 §12.1 的"dx 前是 y、dy 前是 x"口诀，我第一版漏了，靠覆盖度核对抓回来）；
- 全微分观察法补 $\mathrm d(y/x)$、$\mathrm d(\ln\sqrt{x^2+y^2})$、$\mathrm d(\arctan\frac yx)$ 三条常用全微分；
- Euler 的 $x^2y''=D^2y-Dy$ **补全原"整理得"跳过的商法则 + 链式法则两步**；补 $x<0$ 取 $t=\ln|x|$、非齐次项须一并换成 $t$；
- 差分方程补 $\Delta$ 定义与 $a\ne1 / a=1$ 的非齐次特解；
- 5.6 简单应用·三步建模（考纲第 9 条原本只有散落关键词）；
- 4.4 补回 PDF 的两条降阶口诀 callout。

**公式风格**：全部多步推导改用 `$$` + `aligned`（新增 11 个 aligned 块，原文件 0 个）；
中间量用 `\underbrace` 就地命名（如 Bernoulli 的 `z`、Euler 的 `D^2y` 与 `\frac{dt}{dx}`）；
绝对值统一 `\lvert y\rvert`（表格内不出现裸竖线）。

**两个关键教训（重要，别再犯）**：
1. **`Read` 默认只读 2000 行**。原文件 **2155 行**，我第一版漏掉 §37–§40（默写区/答案/背诵版/待补充清单），
   是覆盖度核对脚本报出一批 `\underline{\hspace{...}}` 才发现的。**整理既有长笔记前必须先
   `git show … | wc -l` 核对行数。**
2. **并行编辑同一文件会互相覆盖**：一次消息里发两个 Edit 打同一文件，后写者以旧快照回写，前一处静默丢失。
   同一文件的多处改动必须串行。

**校验**（已固化为技能 `obsidian-daguan-restructure`，含两个脚本）：
- `check_note_structure.py`：`$$` 配对（含引用块 `> $$`）51 对 ✓、aligned 11 对 ✓、left/right 配对 ✓、
  128 行表格列数一致 ✓、同文件与跨文件锚点全部有效 ✓、无裸竖线 / 无控制字符 ✓；
- `coverage_diff.py`:旧 441 个公式块 → 27 条未命中，逐条判读**全是记号级改写**
  （`\lvert…\rvert` vs `|…|`、`\dfrac` vs `\frac`、`\sum` 省上下限、下标 `(x)` 省略、aligned 拆行、
  默写空格长度 12cm→4cm、`dx+dy` 项序）；关键词级反查 49 个术语只缺"二阶微分方程"（已改写为"故为二阶"）
  与"数学二"（写作"数学一、二"）——**无实质内容丢失**。
- 脚本自身踩过 3 个坑并已修：① 逐块精确比对会把搬进 aligned 的公式整块吞掉（改为 `§` 分隔的归一化流子串搜索）；
  ② 归一化必须剥 `&`（aligned 对齐符）与 LaTeX 的 `\ `（反斜杠空格），否则满屏假阳性；
  ③ `\big`→空 的兜底替换会把 `\bigl(` 削成 `l(`，只能用显式 token 表。

## 追加：2.2「线性算子的四条性质」改写为说人话版

用户贴出截屏反馈「这部分请说人话」。**自查结论**：这一节是这次重写里最"照搬 PDF 口吻"的一处——
上来先给"线性微分算子"这个记号、四条性质"已知/结论"两列全是符号，读者得自己翻译。

改法（结构不动，只换讲法）：
1. 开头加 `> [!tip] 先把"算子"这个词翻译成人话`：$L[y]$ 只是给"求两次导、求一次导、各乘系数、加起来"
   这整套动作起的名字（比喻成把"洗、漂、烘"打包叫"洗衣服"），并点明**能打包是因为只含求导与乘已知函数**；
2. 加小标题「一句话：你怎么配系数，右边就怎么变」，并在 aligned 公式下逐行说明
   （第一行是线性、第二行是代已知条件）；
3. 表格由「情形/已知/结论/记忆」四列改为**三列**，去掉冗余的符号"已知"列，换成
   「右边变成什么」——$0+0=0$、$f-f=0$、$f+f=2f$，一眼可核；并**新增一行陷阱**
   "非齐次解自己加自己 $2y$：不是原方程的解，而是 $L[y]=2f(x)$ 的解"；
4. 口诀统一成一句：**系数和是几，右边就是几个 $f(x)$**（$k$ 的推广情形也写出来）；
5. 原「第三条为什么常考」tip 改为 `> [!example]` 具体例子：
   给三个非齐次解，$y_1-y_2$（和 0）、$y_1+y_2-y_3$（和 1）、$2y_1-y_2-y_3$（和 0）分别是什么，
   并写出通解 $C_1(y_1-y_2)+C_2(2y_1-y_2-y_3)+y_3$——**不代回验证、只数系数和**。

校验：结构体检仍全过（`$$` 52 对、aligned 11 对、表格列数一致、锚点有效）；2.2 标题未改，1.3 与 4.3.3 里的两处 `[[#2.2 线性算子的四条性质]]` 不受影响。

**已同步沉淀**：技能 `obsidian-daguan-restructure` 加「⑤′ 说人话检查」硬规则 + 陷阱清单一条；
用户级记忆 `~/.workbuddy/MEMORY.md` 新增「笔记写作：说人话」小节（含"说人话 ≠ 降格"的边界说明）。

## 追加二：mermaid 渲染报错 → 上真解析器；口诀句 → 醒目表格

### 1. 3.1 判型决策树整块渲染成 `Error parsing Mermaid diagram!`

**根因**：节点标签 `E{能否整理成 y'+P(x)y=Q(x)?}` —— **`{}` 里的圆括号 `(` `)` 是 mermaid 语法错误**
（`(` 被当成新形状的起始）。这写法在原笔记里就存在，一直坏着没人发现；我重排时照搬了。

**修法**：照本库 `线代大观` 已有的正确写法 —— **给标签整体加引号**
`E{"能否整理成 y'+P(x)y=Q(x)?"}`（`线代` 里 `R["秩 r(A) = 独立方向数"]` 就是这么写的）。

**新增工具（重要，以后别再靠眼睛看流程图）**：
`~/.workbuddy/binaries/node/workspace/check_mermaid.mjs`（+ 技能内的 `scripts/check_mermaid.sh` 包装）
用 **mermaid + jsdom 真解析**每个 ```mermaid 块，失败时逐行定位到具体是哪一行。
两个坑：① mermaid 的 `parse()` 会调 DOMPurify，Node 下**必须先建 jsdom 的 window/document，
且 mermaid 必须动态 `import()`**（静态 import 会提升到 globals 之前，报 `DOMPurify.sanitize is not a function`）；
② 脚本 cd 到 workspace 解析依赖，故用 `MERMAID_CWD` 把调用目录传进去，相对路径才可用。

**扫描结果**：`04_数学大观` 全库 14 文件 / 19 块 → 修前 1 坏，修后 **0 坏**；
推到整个仓库 36 文件 / 67 块 → **0 坏**。结论：全库只有我重写的那一棵树坏了。

### 2. 「只剩一句话要背」那句分号串 → 醒目表格

用户要求"把这段话整理成醒目的表格"。原写法是一句话里塞三个 `；`：
「系数和 $0$ → 齐次解；系数和 $1$ → 原非齐次方程的解；系数和 $k$ → …」。
改为 **3 行表**：`系数和 Σaᵢ | 右边变成 | 得到的是什么`，结论列按本库既有配色标高亮
（`#c0392b` 齐次解 / `#1e8449` 原非齐次解），`k` 那行保留"**不是**原方程的解"的警告。
**刻意不加"算例"列**——下方 `[!example]` 已经在走 $y_1-y_2$、$y_1+y_2-y_3$、$2y_1-y_2-y_3$，
加了就是同一事实写两遍。

### 3. 沉淀

技能 `obsidian-daguan-restructure`：⑤′ 增至**四条**硬规则（新增"结论/口诀串一律做成表格 + 配色，
且不与下方例题重复算例"）、⑦ 静态体检增加 mermaid 真解析一步、陷阱清单加"mermaid 括号坑"。
用户级记忆加第 ④ 条做法。

## 追加三：并入题型讲义 13 例（含新题型的"查漏补缺"）＋ 判型树裁字修复

源：用户 5 张截图——**微分方程题型讲义**，话题 1（$y'=f(ax+by+c)$）、
话题 2（**分式线性型**）、话题 3（各种灵活换元），共 13 例。

### 1. 真正的缺口：分式线性型（原 PDF 与笔记都没有）

新增 **`3.7 分式线性型：先消常数项`**（`y'=f((ax+by+c)/(a₁x+b₁y+c₁))`）：
- 三个特殊情况表：$c=c_1=0$ → 齐次；系数成比例 $a/a_1=b/b_1=k$ → 令 $z=a_1x+b_1y$ 化可分离；$b=-a_1$ → 全微分；
- 一般情形：令 $x=X+h,\ y=Y+k$，解 $2\times2$ 方程组消常数 → 齐次。**并给了"人话"解释**：
  分子分母是两条相交直线，$(h,k)$ 就是把原点平移到交点；平行（无交点）就落回系数成比例那条；
- 例题：例 4（部分分式）、例 5（+ 全微分**后悔药**；两条路结果同一条曲线，可当场自检）、
  例 6（二次式不可约 → $\ln+\arctan$，简）、例 7（15 届初赛，$u=x^2,v=y^2$ 伪装成标准型，全）。
- 顺带把 **3.1 判型树新增"两个一次式的商"菱形**、总览第③步、6.1 速查表、MOC 导航/速查/知识图全部接上。

### 2. 例题落位（按教学价值详略分明）

全解析 9 例：1、2、4、5、7、8、10、11、13；简（关键变形＋答案）4 例：3、6、9、12。
分布：3.4 ← 例 13（14 届初赛 Bernoulli）；3.5.1 ← 例 1、3、8（**数一 2024**）、9；
3.5.2 ← 例 2（两次换元）、12；3.5.3 ← 例 11；3.6.3 ← 例 10（$u=x^2$ 倒换）；
3.7 ← 例 4、5、6、7。MOC 增「例题索引」表（含出处与详略标记）。

### 3. ★ 发现讲义三处笔误（全部符号级，已按验算结果书写）

用 sympy 逐例验算 13 例（**14 项全过**）：
- **例 5「后悔药」** $\mathrm dy$ 项符号：写成 `+ (x−y+4)dy` 得 $+8y$，对应的是 $y'=-\frac{x+y-2}{x-y+4}$，
  与原题不是一个方程。**对照断言证实**：该版本残差 $=8x+8y-16\ne0$。
- **例 11** 积分因子内外写反：讲义给 $\tan y=e^{x^2}(x^2+C)$；正确是 $e^{-x^2}(x^2+C)$
  （代回 $u'+2xu$ 得 $2xe^{x^2}(2x^2+2C+1)\ne2xe^{-x^2}$）。
- **例 13** 中间步骤同样把 $e^{-\int P}$ 写成 $e^{+\int P}$（最终答案 $(x+C)\cos y=\ln x$ 是对的）。
- 三处都在原笔记位置留了 `[!warning]` 易错点 + **自检办法**：
  隐式解验 $-\dfrac{F_x}{F_y}$ 是否等于题目 $y'$；线性解代回看等式是否成立。

### 4. 判型树"最后一个字被裁掉"的真因与修法

根因**不是文字太长**：mermaid 用**默认字体**量宽度、却用库里实际配置的 **Quiet Sans** 渲染，
宽度差几像素 → 末字换行 → 被 `foreignObject` 裁掉。
**判据极准：被裁的全是含 Latin/数字的标签（`x,y?`、`3.5.2`、`ax+by+c`），纯中文标签一个都不坏。**

修法两件一起上：
1. 每个 mermaid 块首行加 init 指令钉死字体
   `%%{init: {"fontFamily": "Quiet Sans, sans-serif", "themeVariables": {"fontFamily": "Quiet Sans, sans-serif"}}}%%`
   （字体名抄 `.obsidian/snippets/quiet-notes-unified.css` 的 `@font-face`；CSS 里**没有** mermaid 规则，所以是继承来的）；
2. 标签缩短，节号用 `<br/>` 挪第二行。

已加到：`微分方程大观.md` 3 块 + `微分方程 MOC.md` 2 块。
**同库其他专题（一元积分/函数极限/多元微分 MOC、线代大观 9 篇）也有含 Latin 标签的图，同样有裁字风险，尚未处理**——
已向用户说明，待其决定是否一并加这一行指令。

### 5. 沉淀

- 技能新增 **⑧′ 例题验算**（含"残差里带 $F$ 因子 → 先把 $C$ 解出来回代"的假阴性陷阱，
  实测 13 例直接比对挂 3 个、约化后 0 个）与 **⑧″ 图表可读性**（字体钉死 + 短标签）；
  新增脚本 `scripts/verify_examples.py`；description 补触发词（查漏补缺／例题整理／说人话／醒目的表格／图渲染有问题）。
- 环境事实：**sympy 1.14 装在 `/Users/heyuhang/.workbuddy/binaries/python/envs/default/`**（本次新建 venv 装的）。

**体检**：`微分方程大观.md` 1644 行 / `$$` 88 对 / aligned 18 个 / 87 个标题；
MOC 124 行。结构体检全过（含新增的 3.7 锚点、MOC 里 6 条跨文件锚点），mermaid 5 块 0 失败。

## 追加四：★ 图表方案换血——放弃 mermaid，改用 HTML/CSS 组件

用户反馈"展示还是有很多问题，字体很多都看不完整，没有其他方案，比如使用 obsidian 插件重新绘制图案"。

### 1. 上一轮的修法被证伪

上一轮加的 `%%{init: {"fontFamily": "Quiet Sans…"}}%%` **实测没用**。已回改两处记忆里那条错误结论。
真实根因：**mermaid 按自己的字体宽度表估算节点尺寸，再用库里的 Quiet Sans 渲染**，差值导致末字换行被裁。
缩短标签只是缓解，**只要还用 mermaid 就会复发**。

### 2. 换成 HTML/CSS（真解）

新增组件写进 **`.obsidian/snippets/quiet-notes-unified.css` 第十一节**（该文件原 39 对花括号，现 51 对，已校验平衡）：
`.dp` / `.dp-row` / `.dp-ask`（问题胶囊）/ `.dp-yes` / `.dp-ans`（结论胶囊，`.dp-sub` 为次级灰底）/
`.dp-no`（`↓ 否` 分隔）/ `.dp-cards` / `.dp-card` / `.dp-chip`。
配色全用 Obsidian 主题变量（`--background-secondary`、`--interactive-accent`、`--text-on-accent`、
`--background-modifier-border/hover`、`--text-muted/faint`）→ **自动适配明暗主题**。
关键：盒子是 `inline-block`，**由文字撑开，原理上不可能裁字**。组件在 snippet 里，**全库可复用**。

### 3. 五张图全部替换（该专题已 0 个 mermaid）

| 原 mermaid | 换成 | 说明 |
|---|---|---|
| 总览·全章知识地图 | `.dp-cards` 四张卡 | 分叉关系用并列卡片；"解的结构定理支撑待定系数"改为 tip 里一句话 |
| 3.1 判型决策树 | `.dp` 八行流程 | 主战场；顺带把标签里的 `(x)` 等冗字去掉 |
| 4.1 高阶方程总分类 | **删图，并入表** | 图与紧邻的表内容重合，纯装饰；改为 3 列表（子类／节号／卷种）+ 判据 tip |
| MOC 知识结构 | `.dp-cards` | — |
| MOC 解题总顺序 | **删图，改指针** | 这是第三棵同内容的树，违反去重原则；改为 3 行摘要 + 指向 3.1／总览／4.1 |

### 4. 新增工具与约定

- 生成器 `scripts/make_dp_flow.py`：`问 | 答` 清单 → `.dp` 流程；`--cards` → `.dp-cards` 卡片。已测通。
- 体检脚本 `check_note_structure.py` 增两项：**`<div>`/`<span>` 标签配对**、**`.dp` 块内不得有空行**
  （Obsidian 遇空行会断开 HTML 块）。输出新增"HTML 组件数"。
- 约定：**HTML 块前后各留一个空行、块内不留空行**。

### 5. 待用户决定

一元积分 / 函数极限 / 多元微分 MOC、线代大观 9 篇等现存 mermaid 图**同样有裁字风险**（标签都含 `r(A)`、`λ`、`AC-B²` 之类），
本轮未动，已告知并给出两条路线：① 照本文办法批量换 HTML/CSS（推荐，机械可批量）；
② 用**已安装的 Excalidraw 插件**重绘（手绘风、不存在裁字，但图会成为独立文件）。

**体检**：结构体检全过（含 HTML 自检），两个文件 **0 个 mermaid 残留**；
`微分方程大观.md` 1611 行 / 88 对 `$$` / 18 个 aligned / 65 处 HTML 组件；MOC 103 行 / 27 处组件。

## 追加五：删掉正文里的"过程性说明"（用户要求）

用户贴出 `微分方程大观.md` 开头的 `[!info] 整理说明` 截图，要求"把这些过程性说明删除"。
**判据**：重排／去重／改名／并入例题／换图表引擎，这些是**编辑过程**，属于 git 历史与记忆，
**不属于复习材料**。

删除清单：
- 大观 `整理说明` 里两段带日期的改动日志（`2026-09-18 结构重整`四处动刀①②③④、
  `2026-09-18 并入题型讲义例题（13 道）`＋详略＋验算＋笔误说明），约 14 行；
- MOC `来源` 里同性质的两段（结构重整、并入例题），约 4 行；
- 3.1 tip 里"这张图用 HTML/CSS 画、不再用 mermaid、mermaid 会裁字"那一段技术说明；
- 3.7 的 `[!note] 这一节补的是原 PDF 没有展开的题型` → 改写为**面向内容**的
  `[!note] 它和「整体换元」的区别`；
- MOC 的 `## 例题索引（并入的 13 道题）` → `## 例题索引`；
  `[!warning] 三处讲义笔误（本笔记已按验算结果书写）` → `[!warning] 三处最容易写错的地方`。

**保留**（对读者长期有用，不是过程）：
- 来源 PDF、口诀改写说明、**「补充说明」记号约定**、"待题型触发"约定；
- 新增一条**例题详略约定**（"多数给完整解析，少数变体只给关键变形与答案，索引见 MOC#例题索引"）；
- 例题内部的**易错点警告**（例 5／11／13 那三处）——那是内容；
- 4.6 与 5.5 的"原 PDF 只给提醒、未给统一公式"——那是**覆盖边界**说明；
- frontmatter 的 `updated: 2026-09-18`。

**体检**：`微分方程大观.md` 1594 行（-17）/ MOC 98 行（-5）；结构体检全过，
跨文件锚点（含新写的 `[[微分方程 MOC#例题索引]]`）有效，HTML 组件检查通过。
**沉淀**：技能 ⑤′ 新增**第 5 条硬规则**"正文里不留过程性说明"（含三类该留什么、什么要改写成内容导向）；
用户级记忆同步加一条。

## 追加六：例题答案与解析全部折叠

用户要求"把所有例题的答案和解析折叠起来"——目的是**先自己做再看答案**。
关键判断：**不能把整个 `[!example]` 块折叠**（那样连题干都看不见了），
而是**题干留在外面、解析下沉一层**挂成嵌套折叠块 `> > [!success]- 点开看解析`。

做法：
- 写脚本 `fold_callout_answers.py` 批量处理规整块（7 个：例 13／1／8／2／11／10／4）——
  切分规则是"标题行 + 题干，直到第一个空引用行 `>`"，其后整体加 `> > ` 前缀。
- 手工补 5 处：
  - **2.2 节** `[!example] 一个例子解决一类题`：题干后面紧跟正文（没有空引用行），脚本会漏掉；
    顺手把题干从"直接看系数和："改写成真正的问句；
  - **例 5**：原本已含 2 层嵌套（后悔药 + dy 符号警告），直接下沉会到 **4 层** →
    把内层两道 callout 改成**粗体标签**（`**另一条路：…**`、`**⚠️ 注意 dy 项的符号。**`），压回 2 层；
  - 三处"详略从简"的 `[!tip]`（例 3＋9、例 12、例 6＋7）→ 升级成 `[!example]`：
    题干并成 bullet、解析合并进同一个折叠块。
- 结果：**12 个 `[!example]` 块、12 个折叠解析**，覆盖全部 13 道例题（两组合并块）。

**踩的坑**：
- 改 3.5.1 那个块时 `old_string` 只包了正文没包标题行，留下 `> [!tip]`＋`> [!example]` 两个叠在一起的
  callout 头 → 整块渲染会废。**自检办法：扫"连续两行 `> [!`"**。
- 3.7 的变体块 `Edit` 字面匹配怎么都对不上（跨十几行公式）→ 改用「按标题定位 + 整块替换」的 Python 切片，
  且新内容先落成临时文件（避免 heredoc 里 `\b`、`\u` 之类转义吃掉公式）。
- 体检脚本的 `$$` 围栏识别原先只认 `> $$`，**不认 `> > $$`**，多级嵌套里的公式其实是"漏检"状态；
  已改成 `re.fullmatch(r"[>\s]*\$\$[>\s]*", ln)`，修后 `$$` 计数 88 → **96 对**（多出的正是原先漏检的）。

**同步**：`整理说明` 与 MOC 例题索引各加一句"解析一律折叠，先自己做再核对"；
技能 ⑤ 新增"例题/默写题一律题干可见 + 解析折叠"整段（含生成器、≤3 层限制、合并小题的做法、脚本漏检规整度要求），
陷阱清单加"长公式块改用切片替换""替换 callout 必须包住标题行"两条。

**体检**：`微分方程大观.md` 1612 行 / 96 对 `$$` / 18 个 aligned / 12 个折叠解析；MOC 98 行；全部检查通过。
