# 2026-09-18

## 微分方程笔记：平铺 40 节 → 六大板块 + MOC（重排 / 去重 / 补缺 / 改名）

**用户诉求**：「进一步整理微分方程方程的笔记，使其更有逻辑」。经确认选定**重排 + 建 MOC + 改名**，
内容层面**只重排去重补缺、不加例题**。

**落地**（`高数大观/微分方程/`）：
- `git mv 微分方程知识点_Obsidian详细笔记.md → 微分方程大观.md`（1241 行），旧名保留在 aliases；
- 新建 `微分方程 MOC.md`（88 行），对齐「一元积分 / 函数极限 / 多元微分」的 MOC 体例；
- 三个兄弟 MOC 的「同库其他专题」互链各补一行 `[[微分方程 MOC]]`，形成闭环。

**新结构**：总览（主线 + 知识地图 + 解题总顺序八步）→ 一、考试大纲与复习定位 →
二、基本概念与解的结构 → 三、一阶微分方程（3.1 判型树 / 3.2 可分离 / 3.3 一阶线性 /
3.4 Bernoulli / 3.5 三种换元 / 3.6 Mdx+Ndy=0 四条出路）→ 四、高阶微分方程 →
五、综合与应用 → 六、速查与易混 → 七、公式默写自测 → 附录：待补充清单 → 相关链接。

**四处动刀**：
1. 判型信息由 **5 处收拢为 2 极**（原来散在判型总流程、Mdx+Ndy 判结构、次数比较法、两张速查表、考场顺序）；
2. 删重复：原 §7 与 §9 的"整体换元"、原 §20 与 §34 的"待定系数表"各重复一遍；
3. 接链：Bernoulli 前移到一阶线性之后；"线性方程解的性质与结构"从原 §4 上提为独立板块「二」，
   与下游「4.3 常系数非齐次」接成"结构 → 待定系数"一条线；
4. 自测合并：原 §37 默写题与 §38 答案相隔约 200 行，改成「七、公式默写自测」的
   `[!question]` + 嵌套 `[!success]-` 折叠块，点开即见答案；原 §39 背诵版收进 6.5，原 §40 待补充清单移文末附录。

**新增/补缺**（都标 `> [!info] 补充说明`）：
- 3.6 **出路一：可分离**（原 §12.1 的"dx 前是 y、dy 前是 x"口诀，我第一版漏了，靠覆盖度核对抓回来）；
- 全微分观察法补 $\mathrm d(y/x)$、$\mathrm d(\ln\sqrt{x^2+y^2})$、$\mathrm d(\arctan\frac yx)$ 三条常用全微分；
- Euler 的 $x^2y''=D^2y-Dy$ **补全原"整理得"跳过的商法则 + 链式法则两步**；补 $x<0$ 取 $t=\ln|x|$、非齐次项须一并换成 $t$；
- 差分方程补 $\Delta$ 定义与 $a\ne1 / a=1$ 的非齐次特解；
- 5.6 简单应用·三步建模（考纲第 9 条原本只有散落关键词）；
- 4.4 补回 PDF 的两条降阶口诀 callout。

**公式风格**：全部多步推导改用 `$$` + `aligned`（新增 11 个 aligned 块，原文件 0 个）；
中间量用 `\underbrace` 就地命名（如 Bernoulli 的 `z`、Euler 的 `D^2y` 与 `\frac{dt}{dx}`）；
绝对值统一 `\lvert y\rvert`（表格内不出现裸竖线）。

**两个关键教训（重要，别再犯）**：
1. **`Read` 默认只读 2000 行**。原文件 **2155 行**，我第一版漏掉 §37–§40（默写区/答案/背诵版/待补充清单），
   是覆盖度核对脚本报出一批 `\underline{\hspace{...}}` 才发现的。**整理既有长笔记前必须先
   `git show … | wc -l` 核对行数。**
2. **并行编辑同一文件会互相覆盖**：一次消息里发两个 Edit 打同一文件，后写者以旧快照回写，前一处静默丢失。
   同一文件的多处改动必须串行。

**校验**（已固化为技能 `obsidian-daguan-restructure`，含两个脚本）：
- `check_note_structure.py`：`$$` 配对（含引用块 `> $$`）51 对 ✓、aligned 11 对 ✓、left/right 配对 ✓、
  128 行表格列数一致 ✓、同文件与跨文件锚点全部有效 ✓、无裸竖线 / 无控制字符 ✓；
- `coverage_diff.py`:旧 441 个公式块 → 27 条未命中，逐条判读**全是记号级改写**
  （`\lvert…\rvert` vs `|…|`、`\dfrac` vs `\frac`、`\sum` 省上下限、下标 `(x)` 省略、aligned 拆行、
  默写空格长度 12cm→4cm、`dx+dy` 项序）；关键词级反查 49 个术语只缺"二阶微分方程"（已改写为"故为二阶"）
  与"数学二"（写作"数学一、二"）——**无实质内容丢失**。
- 脚本自身踩过 3 个坑并已修：① 逐块精确比对会把搬进 aligned 的公式整块吞掉（改为 `§` 分隔的归一化流子串搜索）；
  ② 归一化必须剥 `&`（aligned 对齐符）与 LaTeX 的 `\ `（反斜杠空格），否则满屏假阳性；
  ③ `\big`→空 的兜底替换会把 `\bigl(` 削成 `l(`，只能用显式 token 表。
