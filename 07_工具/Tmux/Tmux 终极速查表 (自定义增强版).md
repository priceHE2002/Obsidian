

> **核心规则 (必须记住)**
> **前缀键 (Prefix)**: `Ctrl` + `b`
> **操作流程**: 按下 `Ctrl` + `b` -> **松开双手** -> 按下具体指令键

---

## 1. 🚀 独家优化快捷键
*(这是在您的配置文件中专门设置的，比原版更符合直觉)*

| 功能 | 指令键 (Prefix 松手后按) | 记忆技巧 |
| :--- | :--- | :--- |
| **垂直分屏** (左右) | **` | `** (Shift + \) | 像竖线一样切开 |
| **水平分屏** (上下) | **`-`** (减号) | 像横线一样切开 |
| **光标移动** | **`h`** / **`j`** / **`k`** / **`l`** | Vim 风格 (左/下/上/右) |
| **安装插件** | **`I`** (Shift + i) | **I**nstall (安装主题/插件) |
| **重载配置** | **`r`** | **R**eload (修改配置后刷新) |

---

## 2. 🔌 会话管理 (后台保活核心)
*(用于“挂起”和“恢复”工作现场，关机也不怕)*

| 场景 | 命令 / 快捷键 | 说明 |
| :--- | :--- | :--- |
| **新建会话** | `tmux new -s <名字>` | 在普通终端输入 |
| **暂时离开 (Detach)** | **`d`** | **最常用！** 程序在后台继续跑 |
| **恢复会话 (Attach)** | `tmux a -t <名字>` | 回到之前的工作现场 |
| **可视化工坊** | **`s`** | 弹出一个列表，按回车进入对应会话 |
| **重命名会话** | **`$`** | 底部变黄，输入新名字 |
| **彻底杀死会话** | `tmux kill-session -t <名字>` | 结束任务时用 |

---

## 3. 🖱️ 鼠标与复制 (进阶)
*(已开启鼠标支持，操作逻辑有所改变)*

* **调整分屏大小**: 鼠标左键拖拽分割线。
* **切换窗口/分屏**: 鼠标直接点击。
* **翻看历史日志**: 鼠标滚轮直接滚动 (按 `q` 退出)。
* **复制文本 (重点)**:
    1.  按住 **`Shift`** 键 (Mac 可能是 `Option/Fn`)。
    2.  鼠标左键划选文本。
    3.  松开鼠标 -> 右键复制。

---

## 4. 📑 窗口管理 (Windows)
*(类似浏览器的标签页)*

| 功能 | 指令键 (Prefix 松手后按) | 说明 |
| :--- | :--- | :--- |
| **新建窗口** | **`c`** | **C**reate New Window |
| **关闭窗口** | **`&`** | 系统会询问确认 (y/n) |
| **切换窗口** | **`n`** / **`p`** | 下一个 / 上一个 |
| **数字跳转** | **`1`** ~ **`9`** | **已配置为从 1 开始编号** |
| **重命名窗口** | **`,`** | 给标签页起名，方便管理 |
| **查找窗口** | **`w`** | 列出所有窗口供搜索 |

---

## 5. 🔲 窗格管理 (Panes)
*(当前屏幕内的分块操作)*

| 功能 | 指令键 (Prefix 松手后按) | 说明 |
| :--- | :--- | :--- |
| **专注模式 (Zoom)** | **`z`** | 最大化当前分屏，再按一次恢复 |
| **关闭当前块** | **`x`** | 关闭当前光标所在的分屏 |
| **显示编号** | **`q`** | 屏幕显示大号数字，按数字键跳转 |
| **交换位置** | **`{`** 或 **`}`** | 将当前分屏向前或向后移动 |

---

## 附录：您的配置文件备份
*(文件位置: `~/.tmux.conf`，如果换新服务器，复制这段代码写入即可恢复所有功能)*

```bash
# --- 基础功能 ---
set -g mouse on
set -g default-terminal "screen-256color"
set -g base-index 1
setw -g pane-base-index 1

# --- 快捷键优化 ---
bind r source-file ~/.tmux.conf \; display "配置已重载!"
bind | split-window -h
bind - split-window -v
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# --- 插件列表 ---
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'dracula/tmux'

# --- 主题设置 (Dracula) ---
set -g @dracula-plugins "cpu-usage ram-usage time"
set -g @dracula-show-powerline true
set -g @dracula-show-flags true
set -g @dracula-show-left-icon session

# --- 初始化 (必须在最后) ---
run '~/.tmux/plugins/tpm/tpm'